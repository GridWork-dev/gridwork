subject: 8db43f5c5dc6bd56ebdcce7f8d2a029e5b5711e41afef61d1b925c9ab549c4a2
reviewer: fresh-context subagent dispatch, independent second-reader lane — no exposure to the implementing session; no commit message, PR body, SPEC, PLAN or kickoff document was read, and the diff was read cold from `git diff --no-renames FETCH_HEAD...HEAD`
session: secondreader-8p-e3-review-fix-2026-08-15
date: 2026-08-15

# Second-reader record

## Scope

`crates/gwk-pty-host/src/kernel_client.rs` — the PTY host's client-side kernel
handshake. The change adds a check in `connect_inner` that refuses the connection
(`HelloRefused` / `UnsupportedVersion`) when the kernel's `HelloAck` names a
`protocol_major` other than `V1`, plus one test with a positive control: the same
fake kernel answering at V1 must still connect. Before this change the match arm
destructured `HelloAck` without looking at `protocol_major` at all — a real gap
now that `gwk_domain::protocol::ProtocolVersion` names a `V2`, since an ack at V2
would decode and be accepted silently.

## Checks

- [x] citations — every non-obvious terminal behavior carries a `Derivation:` marker
      naming a permitted source, and the marker sits on the behavior it actually
      describes. `cleanroom-gate` already proved every cited ID exists in
      `docs/derivation/SPECS.md` or `docs/derivation/CAPTURES.md`; what it cannot check,
      and you can, is whether the citation is TRUE — that the code does what that source
      says, and came from it
- [x] framing — no source-derived framing in comments, names, or structure
- [x] references — no naming of other projects as comparands, and no capture cited by
      path: captures are referenced by their registered ID and SHA-256, never otherwise.
      Record the finding as the CLASS searched and the HIT COUNT — "terminal-tooling
      comparand class (5 terms): 0 hits" — never the query terms themselves. A record
      that lists them publishes the exact strings this check exists to keep out, and
      the record is the one artifact nobody re-reviews. Give the term count only if you
      ran the grep and know it — never reconstruct one. A name already registered in
      ../SPECS.md is a permitted source, not a query term: cite it, and say whether you
      checked the protocol or the implementation
- [x] leak — `./tools/leak-scan.sh` green

## Notes

**citations.** The file carries exactly one `Derivation:` marker, file-level:
`none — original outbound glue over this repository's domain controls and
already-covered kernel codec (gwk_kernel::wire::frame); it does not reproduce an
external framing or terminal protocol.` The change adds no new marker. The `none`
claim was checked against what the change actually adds rather than against its
prior wording: the new behavior operates only on
`gwk_domain::protocol::{ProtocolVersion, KernelErrorCode}` — this repository's own
internal kernel↔host protocol, already covered by that `none` — and asserts
nothing about terminal escape sequences or any external framing. No `none` sits
beside a real citation. `cleanroom-gate --markers` independently reports the file
declares no derivation, exit 0.

**framing.** The new comments assert that this is the same check `gridwork`'s
client makes, for the same reason. Verified as literally true rather than taken on
the comment's word: `crates/gridwork/src/client.rs` carries the same
`protocol_major != ProtocolVersion::V1 → HelloRefused/UnsupportedVersion` check
with the same reasoning. That is a first-party in-repo cross-reference and an
established one — this file's own top-of-file doc already names `gridwork`'s
`Client` at that exact path as the sibling it mirrors. Names and structure read as
this crate's established vocabulary.

**references.** Positive-controlled grep sweeps across five classes:
terminal-multiplexer/session-manager comparand, terminal-emulator/VT-implementation
comparand, PTY-supervision-tool comparand, derivation-by-comparison phrasing, and
capture-cited-by-path. A seeded scratch file confirmed every term list catches its
own seed — 33 of 33 — before any zero was trusted. Against the added lines: 0 hits,
every class. Against the full gated file: 2 hits in the multiplexer class, both the
common English word "screen", pre-existing and untouched here; 2 hits in the
PTY-tool class, both a library-mediated call inside pre-existing test-fixture code,
untouched here and permitted under CLEANROOM.md rule 2; 0 elsewhere. One
self-reference to this repository's own sibling crate, which is not an external
project and is this file's established pattern. No name registered in
`../SPECS.md` appears anywhere in this file, so no protocol-versus-implementation
disclosure is owed.

**leak.** `./tools/leak-scan.sh` → `leak-scan: clean`, exit 0. `check-claims.sh`
run additionally though not required → clean, exit 0. Subject recomputed from
`git diff --no-renames --name-only FETCH_HEAD...HEAD | ./tools/cleanroom-gate.sh --subject`
and matches the filename.

**Beyond the four checks.** `cargo test -p gwk-pty-host --lib kernel_client::`
under the pinned zig/ghostty toolchain: 5 passed, including the new test. The
reader then mutation-tested the guard itself — neutering the version comparison —
to confirm the new test discriminates rather than passes vacuously. It went red
with the expected message, then restored and re-ran green at 5 of 5, working tree
clean afterwards.

No behavior in this change lacks a citable permitted source. It is internal wire
version negotiation, not terminal-protocol behavior, and the file's existing
`Derivation: none` continues to cover it correctly.

**Verdict: CLEAR.**
