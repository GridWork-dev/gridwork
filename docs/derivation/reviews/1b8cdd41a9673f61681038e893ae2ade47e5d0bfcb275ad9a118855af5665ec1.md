subject: 1b8cdd41a9673f61681038e893ae2ade47e5d0bfcb275ad9a118855af5665ec1
reviewer: fresh-context subagent dispatch, independent second-reader lane — no exposure to the implementing session; no SPEC, PLAN or kickoff document for this phase was read, and the diff was read cold from `git diff --no-renames feat/7p-mux-input...HEAD`
session: secondreader-7p-e3-r2-2026-08-09
date: 2026-08-09

# Second-reader record

## Scope

One gated path. `crates/gwk-pty/src/session.rs` adds the child-facing half of the
window-size relay: a get-before-set skip in `resize` (an identical re-set is not
relayed to the child), a new `pull_size` method that reads the PTY's held size and
reflows the grid to it (the read-and-adopt direction), and eight new integration tests
exercising controlling-terminal access, INTR/QUIT/SUSP signal generation, background
read/write access control with and without TOSTOP, SIGWINCH delivery counting across a
real change / identical re-set / real change sequence, and `pull_size` adoption of a
child-moved size.

## Checks

- [x] citations — every non-obvious terminal behavior carries a `Derivation:` marker
      naming a permitted source, and the marker sits on the behavior it actually
      describes. `cleanroom-gate` already proved every cited ID exists in
      `docs/derivation/SPECS.md` or `docs/derivation/CAPTURES.md`; what it cannot check,
      and you can, is whether the citation is TRUE — that the code does what that source
      says, and came from it
- [x] framing — no source-derived framing in comments, names, or structure
- [x] references — no naming of other projects as comparands, and no capture cited by
      path: captures are referenced by their registered ID and SHA-256, never otherwise.
      Record the finding as the CLASS searched and the HIT COUNT — never the query terms
      themselves. A name already registered in ../SPECS.md is a permitted source, not a
      query term: cite it, and say whether you checked the protocol or the implementation
- [x] leak — `./tools/leak-scan.sh` green

## Notes

Subject recomputed and matched: `git diff --no-renames --name-only
feat/7p-mux-input...HEAD | ./tools/cleanroom-gate.sh --subject` →
`1b8cdd41a9673f61681038e893ae2ade47e5d0bfcb275ad9a118855af5665ec1`, against a clean
working tree at HEAD `df8a05a30fa246cafb2e05bf8d026d4be646ea8a`. `--markers` exits 0
over the one marker-owing `.rs` file. No registry row changes in this diff; the two
rows the new markers cite (`POSIX-TERM`, `POSIX-WINSIZE`) are unchanged at the merge
base, so the subject folds in their standing text.

### citations

Both registered URLs were fetched directly and the full page text searched — the
function page `pubs.opengroup.org/onlinepubs/9799919799/functions/tcsetwinsize.html`
and the chapter page
`pubs.opengroup.org/onlinepubs/9799919799/basedefs/V1_chap11.html` — with row scope
enforced both ways: every `POSIX-WINSIZE` claim was checked against the function page
only, every `POSIX-TERM` claim against XBD Chapter 11 only, and no marker claims a
fact that lives on the other row's page.

**`resize` skip (`POSIX-WINSIZE §APPLICATION USAGE (informative)`, new).** The
function page's APPLICATION USAGE section is exactly two sentences, and both are what
the marker says: the first has the attached manager process invoke `tcsetwinsize()`
"to relay the new terminal window size to the foreground process group" ("relay" is
the page's own verb), and the second says that when a process attached to the
subsidiary device calls `tcsetwinsize()`, the manager "should attempt to change the
screen to reflect the new size" — the marker's quoted fragment is verbatim. The
section sits between the page's "The following sections are informative." line and
"End of informative text.", so the `(informative)` label is correct, and the marker
treats it correctly ("it guides the shape of this method, it does not bind it"). The
skip's application of that guidance ("The skip below is that reflection") is presented
as the crate's own move, not as page text.

**`pull_size` (`POSIX-WINSIZE §APPLICATION USAGE (informative)`, new).** "Covers both
directions of a size relay" is accurate: sentence one is the manager-to-child relay,
sentence two is the child-set-size-to-manager reflection, and `pull_size` implements
the second. The marker's "delivers-nothing rule" cross-reference resolves to the
normative DESCRIPTION sentence "No signal shall be delivered if the terminal size was
changed to the same value it had before the tcsetwinsize() call" — present verbatim on
the page — and the conclusion drawn from it ("adopting a size never needs a set") is
worded as this crate's reasoning.

**SIGWINCH counting test (`POSIX-WINSIZE`, new).** Both halves the marker asserts are
explicit, adjacent normative DESCRIPTION sentences on the function page: "If the
terminal size was changed successfully, a SIGWINCH shall be delivered to the
foreground process group associated with the terminal. No signal shall be delivered if
the terminal size was changed to the same value it had before the tcsetwinsize()
call." Neither half is an inference dressed as text. No informative label is owed —
DESCRIPTION is normative.

**Controlling-terminal test (`POSIX-TERM §11.1.1` + `§11.1.4`, new).** §11.1.1's first
listed case is "Opening /dev/tty, or the pathname returned by ctermid(), in order to
obtain a file descriptor for the controlling terminal" — the marker's quoted fragment
is verbatim and in the named section. The marker's labeled split is honest: the
inference that a successful open asserts the ctty exists is flagged as "this crate's
reading of that access path, not a sentence the chapter contains," and a search of
§11.1.1 and §11.1.3 confirms the chapter indeed contains no such sentence. The
§11.1.4 half checks out: "If a process is in the foreground process group of its
controlling terminal, read operations shall be allowed," and SIGTTIN is sent only for
background-group reads.

**INTR / QUIT / SUSP tests (`POSIX-TERM §11.1.9`, three new markers).** All three are
near-verbatim: "Generates a SIGINT signal which is sent to all processes in the
foreground process group for which the terminal is the controlling terminal" (INTR),
the same sentence shape for SIGQUIT (QUIT), and "receipt of the SUSP character shall
cause a SIGTSTP signal to be sent to all processes in the foreground process group for
which the terminal is the controlling terminal" (SUSP). The INTR marker's side claim —
"the standard names the character's JOB, not its value" — is supported in the same
section: "The values for INTR, QUIT, ERASE, KILL, EOF, EOL, and SUSP shall be
changeable to suit individual tastes," which is also what makes the tests' read-the-
byte-from-termios helper the derived behavior rather than a hardcoded byte.

**Background read/write tests (`POSIX-TERM §11.1.4`, two new markers).** The read
half: "Any attempts by a process in a background process group to read from its
controlling terminal cause its process group to be sent a SIGTTIN signal" — sent to
the group, as the marker says. The write-without-TOSTOP half: the section's exception
list says "if TOSTOP is not set ... the process is allowed to write to the terminal
and the SIGTTOU signal is not sent" — the marker's "a background write is ALLOWED" is
the page's own word. The TOSTOP marker: "Attempts by a process in a background process
group to write to its controlling terminal shall cause the process group to be sent a
SIGTTOU signal," with TOSTOP set and no listed exception applying in the test's
setup. All chapter facts, all in the cited section; the chapter page contains no
informative-section boundary, so no informative label is owed on any `POSIX-TERM`
marker.

**Negative claim (winsize field units).** The diff was searched for any assertion of
what unit the `ws_` fields carry: zero hits. The only unit-adjacent vocabulary in the
added lines is "special character" — §11.1.9's own term for the c_cc entries — and the
code equates `(ws_col, ws_row)` with `(cols, rows)` positionally without claiming a
unit for either.

### framing

First-principles throughout. The new comments derive each behavior from the cited
sentence and flag the crate's own reasoning as such where it goes beyond the page
(the ctty marker's labeled "this crate's reading" split; the skip comment's argument
from the delivers-nothing rule). The vocabulary is the cited pages' own — manager,
subsidiary, relay, foreground process group, the special-character names — and the new
test names (`the_intr_character_reaches_the_foreground_group_as_sigint`,
`pull_size_adopts_a_child_moved_size`) continue the file's established sentence-style
voice alongside the pre-existing
`a_child_can_move_the_pty_size_behind_the_grids_back`. The new helpers (`drain_until`,
`grid_contains`, `rows_containing`, `special_code`) match the file's plain-verb helper
style, and the test scripts are ordinary shell built from trap/echo/read with their
mechanisms argued in place (the ACK protocol from shell trap-at-command-boundary
semantics). Nothing reads as carried in from another implementation.

### references

Class and hit count only, per this directory's rule. Searched the full stacked diff
(added and removed lines). Five term lists were positive-controlled against a seeded
scratch file outside the repository before any zero was trusted (5 of 5 controls hit);
the scratch file was deleted after the control run.

- terminal-multiplexer / session-manager comparand class (8 terms): 0 hits. One
  collision-prone common-noun term was additionally checked bare: its single occurrence
  in the diff sits inside the verbatim quote of the cited function page's own
  APPLICATION USAGE sentence — a common noun in permitted-source text, not a project
  reference. 0 comparand uses.
- terminal-emulator / VT-implementation comparand class (10 terms): 0 hits.
- PTY-supervision-tool comparand class (5 terms): 0 hits (the term list was drawn to
  exclude the Rust `.expect(...)` assertion method, which appears throughout the test
  code as an identifier, not a tool name).
- derivation-by-comparison phrasing class (6 patterns): 0 hits.
- capture-cited-by-path class (6 patterns): 0 hits. No capture is cited in this diff
  at all; the file's one capture citation (`CAP-002`, by registered ID) is at the merge
  base and untouched.

No registered `../SPECS.md` name is newly cited by this diff. The two rows the new
markers cite are protocol/specification documents (the POSIX chapter and function
page), and it is the documents that were checked, both fetched from their registered
URLs.

### leak

`./tools/leak-scan.sh` → `leak-scan: clean`, exit 0. `./tools/check-claims.sh` →
`check-claims: clean`, exit 0.

Verdict: PASS. Zero findings. Every new marker's claim was located verbatim or
near-verbatim in the cited document at the cited section, the one informative-section
citation is labeled as informative and sits inside the page's marked informative span,
both labeled inference splits are honest against the fetched text, row scope holds in
both directions, and the diff makes no claim about winsize field units.
