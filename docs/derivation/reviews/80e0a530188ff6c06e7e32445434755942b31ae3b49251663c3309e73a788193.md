subject: 80e0a530188ff6c06e7e32445434755942b31ae3b49251663c3309e73a788193
reviewer: fresh-context subagent dispatch, independent second-reader lane — no exposure to the implementing session; no SPEC or PLAN document for this phase was read, and the diff was read cold from `git diff main...HEAD`
session: 018D1MbfADJLDWzMw9uuHF23
date: 2026-08-03

# Second-reader record

## Scope

Eight gated paths, all under `crates/gwk-pty-host/`: the crate goes from a two-file
skeleton to the kernel-facing half of the host — envelope minting (`envelope.rs`), an
outbound-only kernel Unix-socket client (`kernel_client.rs`), transcript-record
origination with the two builder-less command families (`ingest.rs`,
`dispatch_node.rs`), result classification (`origination.rs`), a direct
`tracing::Subscriber` JSON-line sink (`logging.rs`), a resident binary (`main.rs`), and
a not-installed systemd unit (`gwk-pty-host.service`, not itself marker-owing). PTY
session supervision is explicitly NOT in this change, and the crate root doc says so.

## Checks

- [x] citations — every non-obvious terminal behavior carries a `Derivation:` marker
      naming a permitted source, and the marker sits on the behavior it actually
      describes. `cleanroom-gate` already proved every cited ID exists in
      `docs/derivation/SPECS.md` or `docs/derivation/CAPTURES.md`; what it cannot check,
      and you can, is whether the citation is TRUE — that the code does what that source
      says, and came from it
- [x] framing — no source-derived framing in comments, names, or structure
- [x] references — no naming of other projects as comparands, and no capture cited by
      path: captures are referenced by their registered ID and SHA-256, never otherwise
- [x] leak — `./tools/leak-scan.sh` green

## Notes

Subject recomputed and matched:
`git diff --no-renames --name-only main...HEAD | ./tools/cleanroom-gate.sh --subject`
→ `80e0a530188ff6c06e7e32445434755942b31ae3b49251663c3309e73a788193`, against a clean
working tree at HEAD `9a7baea` (merge base `cd6f3c1`; `main` has moved past the base on
its own side, which the symmetric-difference diff correctly excludes). The gate names
exactly eight engine-adjacent paths; `--markers` reports all eight `.rs` files as
`declares no derivation`, exit 0, and no file pairs a `none` with a real citation.

### citations — eight `none` declarations, each read as a claim and each true

This change asserts no terminal behavior, so per this directory's README every
declaration was checked by reading the file against its stated reason, not by trusting
the syntax:

- No file spawns a process: the only occurrences of "spawn" in the crate are prose
  about what is deliberately absent and one test log-message string. No
  `Command::new`, no `forkpty`/`openpty`, no `tokio::spawn` in `src/`.
- No file emits or parses a terminal-protocol byte: zero escape/CSI/OSC/DCS constants
  anywhere in `src/` (grepped byte and name forms).
- No file binds anything: zero `bind`/`UnixListener`/`TcpListener` in `src/`. The one
  socket type is a `UnixStream` the client CONNECTS.
- The kernel wire is consumed, not re-derived: `kernel_client.rs` imports
  `gwk_kernel::wire::frame::{Budget, Incoming, read_frame, write_frame}` and never
  reimplements framing — the same four imports `crates/gridwork/src/client.rs` uses,
  which is exactly the "mirrors, never reimplements the codec" claim its doc makes.

Because the citations here are `none`, the load-bearing claims live in doc comments
that characterize OTHER in-repo code, and those were verified against that code:

- **Idempotency, end to end (the hardest-checked claim).** `envelope::mint` derives
  `command_id` as `cmd-{key}`; `crates/gridwork/src/lib.rs`'s `envelope()` uses the
  identical scheme, and its `gw ingest submit` key is `{kind}:{sha256-hex}` via
  `digest_of_json` — byte-identical convention to `ingest::ingest_key` (both digest the
  serde serialization; `to_string` and `to_vec` agree). The replay decision in
  `crates/gwk-kernel/src/submit.rs`'s `prior_for_key` compares project, aggregate
  target, actor, and payload — and deliberately NOT `issued_at` and NOT `command_id` —
  so a host restart re-originating identical content with a fresh clock lands as
  `Prior::Replay` of the original events, never a second append and never a spurious
  `IdempotencyConflict`. That is what makes "a retry lands once" true even though every
  envelope carries a new timestamp. The three key families are prefix-disjoint
  (`register_dispatch_node:`/`transition_dispatch_node:`/`{kind}:`), probed
  collision-free.
- `origination::classify`'s premise — a `SubmitCommand` answers only `CommandApplied`
  or `Error` — confirmed in `wire/serve.rs` (request → `store.submit`) plus `submit`'s
  two return paths; `route_of` really is wildcard-free (39 arms, no `_ =>` in the
  function). The quoted `CommandApplied` doc sentence exists verbatim in
  `crates/gwk-domain/src/protocol.rs`.
- `ACTOR_KIND = "host"` is genuinely distinct from the two existing kinds:
  `"operator"` (`crates/gridwork/src/lib.rs`) and `"kernel"` (kernel-authored events).
- The systemd unit against the binary: `Type=notify`/`NotifyAccess=main` matches
  `notify_ready()` (READY=1 datagram on `$NOTIFY_SOCKET`,
  `crates/gwk-kernel/src/wire/listen.rs:293`) called from the main process;
  `RestrictAddressFamilies=AF_UNIX` is consistent with everything the binary does
  (outbound `UnixStream` + the notify datagram, both AF_UNIX, no listener); the
  same-EUID warning matches ADR 0002 and `listen.rs`'s `admits`; `ExecStart` matches
  the `[[bin]]` name; SIGTERM+Ctrl-C handling matches the claimed
  `crates/gridwork/src/admin.rs` posture.
- The stated reason the crate cannot depend on the engine crates is true: all three
  adapter manifests declare `gwk-pty`, and the pinned-toolchain requirement is real
  (`crates/gwk-pty/pins.env`). `cargo tree -p gwk-pty-host --edges normal` shows ZERO
  `gwk-pty` or `gwk-adapter-*` edges — the dependency claim is not just a comment.
  `check_pty_dependents` accordingly does not fire for this crate; it is gated by
  prefix and its explicit row, as the paths file itself says.
- "No adapter crate constructs a `DispatchNodeId`" holds: the three adapter hits for
  that type are an `Option<DispatchNodeId>` passthrough field and a
  `None::<DispatchNodeId>` — no adapter calls its constructor.

**Findings (non-blocking, one class): three stale in-repo file pointers.** The
conventions mirrored are all real, but three doc comments name the wrong in-repo file
for them: (1) five comments cite `crates/gridwork/src/main.rs` for `envelope()`,
`digest_of_json`, `socket_path`, and `rfc3339`/`civil_from_days` — all of those live in
`crates/gridwork/src/lib.rs`; `main.rs` is a 12-line shim. (2) `envelope.rs` cites
`crates/gwk-kernel/src/project.rs` for the kernel's own `"kernel"` actor — that kind is
constructed in `epoch.rs` (genesis) and in tests, not in `project.rs`. (3) the unit
file attributes "SIGTERM handled … READY=1 datagram" to `docs/operations.md`'s
description; that doc says only "Readiness is emitted only after …" and describes the
shutdown drain — the named specifics live in the code. None of these touches an
external source or changes any behavior claim, so none is a clean-room violation;
recommend fixing the pointers in a follow-up. Recorded so the next reader does not
re-derive them.

One precision note, not a finding: `envelope.rs` says the kernel decides CAS "never
from the envelope's" `expected_version`. `check_expected_version` does read the
envelope's field when present — as an agreement assertion against the derived version —
but the CAS decision itself comes from the command body, and this crate sets the
envelope fields `None`, matching the CLI precedent. Honest in substance.

### The named test, and the adjacent shapes it does not cover

`ingest::tests::a_malformed_transcript_record_fails_typed_not_a_panic` runs and passes
in isolation, and the full crate suite passes 23/23 (`CARGO_TARGET_DIR` pointed at the
shared artifact cache; this box carries no Zig, which is itself confirmation the crate
builds without the engine toolchain). Beyond the shipped tests I ran a 9-probe battery
in a temporary integration test (written, run, deleted; tree clean before and after):

- every non-object JSON type (null, boolean, number, array) fails typed with its own
  type name — not just the string case the shipped test seeds;
- the inline size bound is exclusive: a body serializing to exactly
  `INLINE_PAYLOAD_MAX_BYTES` passes, one byte over is refused;
- whitespace-only hint fields are refused, not just empty ones (`dispatch_node_id`,
  `kind`, `to` — the `trim()` is load-bearing and probed);
- insertion order does not change the ingest key (serde_json's sorted map means a
  producer assembling fields in a different order still retries as the same command);
- nesting 600 levels deep — past any serde_json parser's 128-depth admission — does
  not panic;
- the three key families cannot collide with each other (prefix disjointness);
- a fake listener that closes during the hello, one that answers `HelloRefusal`, and
  one that answers a non-hello frame each surface as the matching typed
  `KernelClientError` — never a panic. One probe assertion of MINE was corrected, not
  the code: an immediate peer close can surface as the codec's typed reset error
  rather than the clean `ClosedDuring`, depending on FIN timing; both are typed.

One bounded liveness observation, not a blocker and not clean-room-relevant: the
client has no read deadline, so a kernel that accepts the connection but never answers
the hello stalls startup before READY=1 — under `Type=notify` systemd's start timeout
then kills it, which is a survivable posture and the same one the CLI client takes.

### framing

The functional vocabulary (spawn, pump loop, restart semantics, session registry,
detach/reattach routing) is quoted from CLEANROOM.md rule 2's own category definition
and attributed as such in the crate root doc. Every mirror named in a comment is an
in-repo crate (`gridwork`, `gwk-kernel`, `gwk-domain`, the three adapters); nothing in
names, structure, or comments reads as transcribed from an external implementation.
The one external-project mention is a build-toolchain fact, handled under references.

### references

Class and hit count only, per this directory's rule. Each class was run over two
corpora — the added lines of the full diff, and the eight gated files at HEAD — and the
scanner was positive-controlled against seeded strings from each class before any zero
was trusted.

- terminal-multiplexer / session-manager comparand class (10 terms): 0 hits
- terminal-emulator / VT-implementation comparand class (19 terms): 1 raw hit — the
  pinned engine-library upstream this repository already materializes by pin, named
  once in the crate root doc as a BUILD INPUT of `gwk-pty` (with its Zig version and
  pin-file paths); the same name appears in 10+ files at the merge base including the
  pin file, manifests, and CI. A dependency statement, not a comparand; no behavior is
  attributed to it. 0 comparand uses.
- PTY-supervision-tool comparand class (7 terms): 6 raw hits, every one the Rust
  `.expect(...)` method in test code — 0 project-name uses.
- derivation-by-comparison phrasing class (10 terms): 0 hits
- capture-cited-by-path class (1 pattern): 0 hits

Registered permitted sources: the three engine-CLI names registered in `SPECS.md`
appear only inside this workspace's own adapter-crate and type names (2, 4, and 3
mentions respectively — `gwk-adapter-*`, `Message`/`CodexEvent`/`Event`). They are
named as in-repo crates, not as external protocols or implementations; this diff
asserts no protocol behavior of any of them, so the protocol-versus-implementation
question does not arise. No capture is cited by path anywhere in the change; the two
registries are untouched by this diff.

### leak

`./tools/leak-scan.sh` → `leak-scan: clean`, exit 0. `./tools/check-claims.sh` →
`check-claims: clean`, exit 0.

Verdict: PASS. No escalation — this change asserts no terminal behavior, so there is
no behavior in it lacking a citable permitted source. The three stale in-repo pointers
above are follow-up material, not blockers.
