subject: a447d09f2a2a94f0af1e908c74fb1d1389a823c6379811984cbfd25b7a6f6635
reviewer: gw-general-purpose, fresh-context dispatch — no exposure to the implementing session (routed here per CLEANROOM.md rule 4's requirement for a fresh-context lane; every specialized GridWork agent carries house instructions that would be exactly the prior exposure the control exists to exclude) — round 2
date: 2026-08-01

# Second-reader record

## Scope

Three new engine-adapter crates — `crates/gwk-adapter-{opencode,claude,codex}/src/lib.rs` — each pairing a render half (the engine's native TUI spawned under `gwk_pty::Session`, never typed into) with a control half (the engine's own protocol invocation: `opencode acp` via the ACP SDK config, `claude --print --input-format stream-json --output-format stream-json`, `codex app-server`), plus registry changes in `docs/derivation/SPECS.md`: the `OPENCODE-ACP` row added, the `CLAUDE-STREAM-JSON` row repointed to the CLI reference, and a new uncited `CLAUDE-HEADLESS` row.

## Checks

- [x] citations — every non-obvious terminal behavior carries a `Derivation:` marker
      naming a permitted source, and the marker sits on the behavior it actually
      describes. `cleanroom-gate` already proved every cited ID exists in
      `docs/derivation/SPECS.md` or `docs/derivation/CAPTURES.md`; what it cannot check,
      and you can, is whether the citation is TRUE — that the code does what that source
      says, and came from it
- [x] framing — no source-derived framing in comments, names, or structure
- [x] references — no naming of other projects as comparands, and no capture cited by
      path: captures are referenced by their registered ID and SHA-256, never otherwise
- [x] leak — `./tools/leak-scan.sh` green

## Notes

Round 2 of the review recorded (in round 1) under subject `15017619e0fb…`, which
escalated on the `CLAUDE-STREAM-JSON` citation: the marker claimed `--input-format
stream-json` and a bidirectional stream against code.claude.com/docs/en/headless, a
page that no longer mentions `--input-format` at all. The amendment resolves it
registry-side plus a marker reword, exactly as the escalation suggested. Subject
recomputed locally (`git diff --no-renames --name-only 0513b4a...HEAD |
CLEANROOM_BASE=0513b4a ./tools/cleanroom-gate.sh --subject`) and it matches this
record's filename. All live-document verification performed 2026-08-01.

**citations — pass (all three markers verified TRUE against live documents):**

- `CLAUDE-STREAM-JSON` (`gwk-adapter-claude/src/lib.rs`, on `control_command()`) —
  TRUE against the repointed row's document, code.claude.com/docs/en/cli-reference,
  re-fetched this round. The marker now claims only the flag-pair fact: "print mode
  accepts `--input-format stream-json` (stdin) and `--output-format stream-json`
  (stdout)". On the cited page: the `--input-format` row reads "Specify input format
  for print mode (options: `text`, `stream-json`)"; the `--output-format` row reads
  "Specify output format for print mode (options: `text`, `json`, `stream-json`)";
  and the stdin/stdout association is stated on the same page by the
  `--replay-user-messages` row — "Re-emit user messages from stdin back on stdout for
  acknowledgment. Requires `--input-format stream-json` and `--output-format
  stream-json`" — which names both transports in connection with exactly this flag
  pair. Every element of the marker and of the reworded registry row is on the cited
  page; the round-1 wrong-document failure is closed. The richer "bidirectional
  stream" phrasing survives only in rustdoc prose, not in the derivation claim, and
  the new `CLAUDE-HEADLESS` row that would carry stream semantics is cited by nothing
  in this change — uncited rows are free to add by design.
- `OPENCODE-ACP` (`gwk-adapter-opencode/src/lib.rs`, on `acp_agent_config()` building
  `opencode acp`) — file byte-identical to round 1 (same blob); TRUE, verified this
  session against opencode.ai/docs/acp: "The command starts OpenCode as an
  ACP-compatible subprocess that communicates with your editor over JSON-RPC via
  stdio." The row added in this change matches the document.
- `CODEX-APP-SERVER` (`gwk-adapter-codex/src/lib.rs`, on `control_command()` building
  `codex app-server`) — marker byte-identical to round 1; TRUE, verified this session
  against github.com/openai/codex/blob/main/codex-rs/app-server/README.md: "supports
  bidirectional communication using JSON-RPC 2.0 messages (with the
  `\"jsonrpc\":\"2.0\"` header omitted on the wire)" and "stdio (`--stdio` or
  `--listen stdio://`, default): newline-delimited JSON (JSONL)". Standing note for
  whoever writes the wire codec: the README states the `"jsonrpc":"2.0"` header is
  omitted on the wire.

**framing — pass.** House-symmetric structure across the three adapters; names
(`spawn_tui`, `control_command`, `acp_agent_config`, `engine_id`) and comments speak
this repo's architecture, nothing reads as transcribed from another implementation.
The round-1 note on the codex module doc is addressed: it now says engine-generated
schemas "will be vendored", no longer asserting machinery this change does not
contain. The claude test comment citing "the rendering spike" is an internal
observation, not another implementation.

**references — pass.** No comparand project named (grep over the three crates for
the terminal-tooling comparand class: 0 hits; the round-2 deltas are
comment-wording only). No capture cited at all — all three markers cite SPECS IDs,
none by path. The engines named (claude, codex, opencode) are the subjects being
adapted; `agent-client-protocol` and ghostty appear only as declared dependencies.

**leak — pass.** `./tools/leak-scan.sh` → "leak-scan: clean", exit 0.

**Verdict: PASS.** All four checks pass; the round-1 escalation is resolved by the
repointed row and narrowed marker, both verified against the live document.
