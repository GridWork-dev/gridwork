subject: d07f2e11a15a699ca52682ff60ec4d1df9aa2557d786eab95ca3b9861451624d
reviewer: fresh-context subagent dispatch, independent second-reader lane — no exposure to the implementing session; no SPEC, PLAN or kickoff document for this phase was read, and the diff was read cold from `git diff --no-renames feat/7p-mux...HEAD` — a stacked base (`feat/7p-mux`, not `main`)
session: secondreader-7p-e9-r1-2026-08-09
date: 2026-08-09

# Second-reader record

## Scope

Three gated paths under the console's multiplexer half. `crates/gwk-tui/src/workspace/
input.rs` is new: the workspace input grammar — pass-through with one intercepted
leader, a one-shot command mode, a typed palette with prefix completion — plus an
`Action`/`apply` bridge onto the workspace model and the `DEFAULTS` lineage table
(every default keybinding and command name with a registry-row-or-`original` lineage
cell). `crates/gwk-tui/src/workspace/mod.rs` gains `pub mod input;` and rewords its
"what is deliberately absent" doc to point bindings at the new module.
`crates/gwk-tui/src/workspace/render.rs` gains `render_input` — a separate layer
painting the mode word and the palette overlay (input line, candidates, notice, and
the `list-keys` lineage-table view) through the ratatui buffer — plus tests. This is a
STACKED change: the diff base is the local branch `feat/7p-mux`, whose own gated
change was separately reviewed and is not this record's subject.

## Checks

- [x] citations — every non-obvious terminal behavior carries a `Derivation:` marker
      naming a permitted source, and the marker sits on the behavior it actually
      describes. `cleanroom-gate` already proved every cited ID exists in
      `docs/derivation/SPECS.md` or `docs/derivation/CAPTURES.md`; what it cannot check,
      and you can, is whether the citation is TRUE — that the code does what that source
      says, and came from it
- [x] framing — no source-derived framing in comments, names, or structure
- [x] references — no naming of other projects as comparands, and no capture cited by
      path: captures are referenced by registered ID and SHA-256, never otherwise.
      Record the finding as the CLASS searched and the HIT COUNT — never the query terms
      themselves. A name already registered in ../SPECS.md is a permitted source, not a
      query term: cite it, and say whether you checked the protocol or the implementation
- [x] leak — `./tools/leak-scan.sh` green

## Notes

Subject recomputed and matched: `git diff --no-renames --name-only feat/7p-mux...HEAD |
./tools/cleanroom-gate.sh --subject` →
`d07f2e11a15a699ca52682ff60ec4d1df9aa2557d786eab95ca3b9861451624d`, against a clean
working tree at HEAD `5a8da965f680c5b2c48bff9a2f10d19de3373986`. `--markers` over the
same input: `input.rs` cites `TMUX-MANUAL ZELLIJ-PRESETS ZELLIJ-KEYBINDS POSIX-VI`;
`mod.rs` and `render.rs` declare no derivation; exit 0. The full gate (with
`CLEANROOM_BASE` at the merge base) failed on exactly one thing before this record
existed: the record's own absence. `./tools/check-claims.sh` → clean.

### citations

All four cited registry rows were checked against their registered documents, fetched
directly from the URLs the `SPECS.md` rows give — the documents only, never any
implementation's source.

**`TMUX-MANUAL §DEFAULT KEY BINDINGS` (input.rs:11).** Fetched `man.openbsd.org/tmux.1`.
The section opens verbatim: controlled "by using a key combination of a prefix key,
'C-b' (Ctrl-b) by default, followed by a command key", followed by a table binding one
key to one action each — the marker's "one leader key, then one command key, selects
one action", with the one-shot reading entailed by that grammar. The marker deliberately
does NOT claim the manual's `C-b` for this code's entry key — `Ctrl-g` is cited to
`ZELLIJ-PRESETS` instead, an honest split. `resize-pane` (6 occurrences) and
`list-keys` (7) are confirmed named commands in the manual, matching the row's
command-vocabulary clause. The code embodies the grammar: `handle_command` sets the
mode back to pass-through on entry so any single command key ends it (input.rs:544),
pinned by `input_the_command_mode_is_one_shot`.

**`ZELLIJ-PRESETS §Unlock-First (non-colliding) preset` (input.rs:15).** Fetched
`zellij.dev/documentation/keybinding-presets.html`. The Unlock-First section states the
preset is for users whose terminal applications carry colliding shortcuts (the marker's
"chosen there to avoid colliding with keys running programs expect" is an accurate
paraphrase), that the interface is unlocked with `Ctrl g`, that what follows is
single-character ("Ctrl g followed by p followed by n"), and that example itself shows
`n` opening a new pane. `x` close focused pane is stated one paragraph up on the same
registered page, in the default-preset parenthetical ("n to open a new pane or x to
close the focused pane") — and the row text itself carries the n/x pairing.
Non-blocking precision note: the header marker groups the `x` verb under its
§Unlock-First pointer while the page states it in the default-preset paragraph; the
claim is what the registered document says (`SPECS.md`: the section is for the reader,
the gate resolves the ID), the two presets share the pane-mode verbs, and the
`DEFAULTS` cell for `x` cites the row without a section. Code: `Ctrl-g` is the one
intercepted key in pass-through (input.rs:533); `n`/`x` select split/close
(input.rs:559, 561).

**`ZELLIJ-KEYBINDS §keybinds` (input.rs:21).** Fetched
`zellij.dev/documentation/keybindings.html`. The page's `keybinds` block carries its own
comment "keybinds are divided into modes", with `bind` instructions mapping keys to
named actions — exactly the marker's mode-division convention and nothing more. Code:
the `Mode` enum, per-mode dispatch, and the named `Action` union; the `DEFAULTS`
table's mode column states the same division as data.

**`POSIX-VI §EXTENDED DESCRIPTION` (input.rs:24).** Fetched
`pubs.opengroup.org/onlinepubs/9799919799/utilities/vi.html`. EXTENDED DESCRIPTION
opens: "vi shall be in command mode; text input mode can be entered by one of several
commands" — the command-keys-act / typed-text-is-text distinction the palette rides.
Its command descriptions give `h` Move Cursor Backwards, `j` Move Down, `k` Move Up,
`l` Move Cursor Forward — the marker's left/down/up/right order — and `:` "Execute an
ex Command", a typed command line. Code: `h`/`j`/`k`/`l` → FocusLeft/Down/Up/Right
(input.rs:562-565), `:` opens the palette (input.rs:554-557), palette characters append
as text and Enter resolves (input.rs:612-631).

**Both `none` declarations re-verified TRUE.** `mod.rs` remains a pure weighted-tree
model with an integer geometry solver; this diff's edit is a module-doc reword plus
`pub mod input;` — still no process spawned, no byte parsed or emitted, no
colour/SGR or keyboard-protocol fact, and the `none` sits beside no real citation.
`render.rs` still paints every cell through ratatui's buffer API, including the new
`render_input` mode word and palette overlay — rule 2's library-mediated exemption —
and its new tests drive `InputState` through crossterm's typed `KeyEvent` API, the same
exemption; painting key names as table text asserts no keyboard-protocol fact.
`input.rs` likewise reads key events only through crossterm's typed API — no
hand-authored escape parsing anywhere in this diff.

**The `DEFAULTS` lineage table.** 23 rows: 7 lineage cells start with a registered row
ID (`TMUX-MANUAL` on the leader row, `resize-pane` and `list-keys`; `ZELLIJ-PRESETS`
on `n` and `x`, plus the leader row's second clause; `POSIX-VI` on the motion keys and
`:`) and 16 start with `original`. Each ID cell was checked against its row text and
the fetched document above and claims only what the row states; the mixed cells narrow
rather than widen ("the columns-axis default is original", "the argument words are
original"). Zero cells reference the project this repository never cites — confirmed
by reading all 23 cells; the module's own test additionally pins the cell shape,
rejects any cell carrying that project's name-word, and proves its predicate can go
red with a fabricated row ID and a plain-prose control.

### framing

No source-derived framing. The module doc separates adopted convention from original
design in so many words ("Palette design is original work"; "the rest are this model's
own nouns"), names come from this model's own vocabulary (workspace/tab/pane,
columns/rows, grow/shrink, `Outcome::Forward`/`Handled`/`Act`), and the state machine
is plain Rust idiom shaped by the cited conventions, not by anyone's implementation.
The comments argue from first principles ("a stray key cannot leave the surface
half-armed"; "the overlay covers panes, not blends into them"). One refusal-path test
types a command name that is part of the `TMUX-MANUAL` row's registered vocabulary
(`detach-…`) precisely as a command that does NOT exist in this module — a fixture for
the no-such-command notice, not a comparand.

### references

Class and hit count only, per this directory's rule. Searched the three gated files,
full-file scope. Every term list was positive-controlled against a seeded scratch file
before any zero was trusted (30 of 30 controls hit); the seed was deleted.

- terminal-multiplexer / session-manager comparand class (6 terms): 8 hits, all
  false-positive on one generic display-vocabulary word — five in `render.rs` tests (a
  variable naming the painted display, plus an on-display assertion message), one in
  `mod.rs` doc prose (a direction described in display terms), and two in `input.rs`'s
  own lineage-gate test: the exclusion predicate that rejects any lineage cell carrying
  the never-cited project's name-word, and its seeded-red fabricated-row-ID control.
  0 comparand uses.
- terminal-emulator / VT-implementation comparand class (8 terms): 0 hits.
- modal-editor comparand class (5 terms): 0 hits.
- derivation-by-comparison phrasing class (9 terms): 0 hits.
- capture-cited-by-path class (2 terms): 0 hits — this diff cites no captures at all.

Registered-source disclosure: `TMUX-MANUAL`, `ZELLIJ-PRESETS`, `ZELLIJ-KEYBINDS`, and
`POSIX-VI` are newly cited by this diff. For each I checked the registered DOCUMENT at
the row's URL — the tmux manual page, the two Zellij User Guide pages, the POSIX vi
utility page — never any implementation's source. The two project names appear in the
gated files only inside their row-ID citations: every occurrence sits on a line
carrying the row ID (`TMUX-MANUAL` 5, `ZELLIJ-PRESETS` 5, `ZELLIJ-KEYBINDS` 2,
`POSIX-VI` 4).

### leak

`./tools/leak-scan.sh` → `leak-scan: clean`, exit 0. `./tools/check-claims.sh` →
`check-claims: clean`, exit 0.

Verdict: PASS. One non-blocking precision note (above): the header marker's
§Unlock-First pointer groups in the `x` pane verb that the registered page states in
its default-preset paragraph; the claim itself is what the document says, and the
`DEFAULTS` cell cites the row without a section. Every other citation sits exactly on
the section it names; the lineage table's ID cells all check out against their rows
and documents; both `none` declarations remain true of the edited files.
