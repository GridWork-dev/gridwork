subject: a1fbd7ab5258b663441237faad78a26cc3fb1e97a0bfaa568547fbfb8b5dbc32
reviewer: fresh-context subagent dispatch, independent second-reader lane — no exposure to the implementing session; no SPEC, PLAN or kickoff document for this phase was read, and the stacked diff was read cold from `git diff --no-renames feat/7p-mux-detach...HEAD`
session: secondreader-7p-e2-r1-2026-08-09
date: 2026-08-09

# Second-reader record

## Scope

Two gated paths, both declaring `Derivation: none`, so both declarations were re-checked
against the files rather than trusted. `crates/gwk-tui/src/workspace/arrange.rs` is new:
a pure, deterministic rebuild of the client's `WorkspaceState` from the kernel's
`workspace_node` projection rows (`gwk_domain::entity::WorkspaceNode` — first-party
types over the first-party contract schema), returning the model, a per-pane ledger
binding, and the rows it could not legally place. `crates/gwk-tui/src/workspace/mod.rs`
changes by one doc paragraph (the persistence note now names the projection as the
durable authority) plus the `pub mod arrange;` line; the rest of the file is untouched
by this diff and its existing `Derivation: none` declaration is unchanged.

## Checks

- [x] citations — every non-obvious terminal behavior carries a `Derivation:` marker
      naming a permitted source, and the marker sits on the behavior it actually
      describes. `cleanroom-gate` already proved every cited ID exists in
      `docs/derivation/SPECS.md` or `docs/derivation/CAPTURES.md`; what it cannot check,
      and you can, is whether the citation is TRUE — that the code does what that source
      says, and came from it
- [x] framing — no source-derived framing in comments, names, or structure
- [x] references — no naming of other projects as comparands, and no capture cited by
      path: captures are referenced by their registered ID and SHA-256, never otherwise.
      Record the finding as the CLASS searched and the HIT COUNT — never the query terms
      themselves. A name already registered in ../SPECS.md is a permitted source, not a
      query term: cite it, and say whether you checked the protocol or the implementation
- [x] leak — `./tools/leak-scan.sh` green

## Notes

Subject recomputed and matched: `git diff --no-renames --name-only
feat/7p-mux-detach...HEAD | ./tools/cleanroom-gate.sh --subject` →
`a1fbd7ab5258b663441237faad78a26cc3fb1e97a0bfaa568547fbfb8b5dbc32`, against a clean
working tree at HEAD `507df6d7bc44bc7e2266bc16f95d3a5841807e02`. `--markers` over the
same file list exits 0, reporting both files as declaring no derivation.

### citations

**`crates/gwk-tui/src/workspace/arrange.rs` (new, `Derivation: none`).** The declaration
was checked the way a `none` demands — by reading every line and asking whether the file
does any of the things a `none` forbids. It does none of them: it spawns no process,
parses and emits no byte (its only inputs are typed `WorkspaceNode` structs, its only
outputs the in-repo model types), supervises no session (`PtySessionId` passes through
as an opaque id from row to binding and is never dereferenced), authors no escape
sequence, maps no key, and asserts no external terminal behavior anywhere. Imports are
`std::collections`, `gwk_domain` (in-repo), the parent module's own model types, and —
in tests only — `ratatui::layout::Rect`, the crate's openly declared TUI dependency.

The declaration's positive claim — that the mapping rules are the client's own choices —
was verified against the first-party sources each argument leans on:

- *Sibling order `(created_at, id)` is "the client's own choice" because "the projection
  carries no order column."* TRUE: `schema/0001_contract.sql`'s `workspace_node` table
  carries exactly `id, version, kind, parent_id, session_id, created_at, updated_at` —
  no order column, and no geometry columns either, which is what makes the module doc's
  "everything the rows do not carry is transient geometry" a fact of the schema rather
  than an assertion about any external tool.
- *The parent-leaf-first rule* is argued from the model and the schema, and both halves
  check: the schema's `workspace_node_session_only_on_pane` constraint means the parent
  row of a pane-under-pane split can itself carry a session, and the model binds
  sessions to leaves, so the parent must stay a leaf; the code hard-places the parent's
  leaf first in the split rather than depending on the ordering gloss.
- *The husk-placeholder rule* is argued as model invariant versus ledger legality, and
  both halves check: `mod.rs`'s merge-base doc states the every-container-holds-a-member
  invariant (and "husk" is that pre-existing doc's own word), while nothing in the
  schema requires a workspace to hold a tab or a tab a pane — rows arrive one command at
  a time, so the gap the placeholder bridges is real.
- *The legality assumptions behind `ignored`* restate the schema's own trigger:
  `assert_workspace_parentage` refuses a parentless tab or pane, a parented workspace,
  and a pane under a workspace — exactly the shapes the tests feed in and the code
  reports; a duplicate id is refused by the primary key. "Foreign shapes the kernel
  itself would have refused" is a first-party fact, verified.

**`crates/gwk-tui/src/workspace/mod.rs` (`Derivation: none`, doc-paragraph diff).** The
new paragraph asserts one first-party fact — the kernel's `workspace_node` projection is
the durable arrangement authority and `arrange` rebuilds from its rows — verified against
the schema and against `arrange.rs` itself, whose `reproduce` takes rows and nothing
else. No code changes; the declaration REMAINS true of the file as it now stands.

Two non-blocking notes. First, a test comment cites an internal ruling by ID ("ruling
S2"); the ruling document is phase material this reviewer is barred from and did not
read, but the property the test enforces — client-made geometry cannot survive a rebuild
because geometry is not a ledger fact — is independently verified against the schema, so
nothing in this verdict rests on the ruling's text. Second, the "parent first because it
is necessarily the older row" gloss: the foreign key does force a parent's insertion to
precede its children's, but equal `created_at` values are possible in principle; the
code does not depend on the gloss (it places the parent's leaf first explicitly), so
this is a prose nicety, not a defect.

### framing

First-principles throughout. The structure is the obvious one for the job — index rows
by id, group children under parents, sort, walk from legal roots, sweep the unplaced —
and every mapping decision is argued in place from this repository's own schema and
model invariants, not asserted as how such things are done. Names (`reproduce`,
`Reproduced`, `PaneBinding`, `Builder`, `mint`, `kids`, `ignored`, placeholder/husk) are
plain descriptions or the parent module's established vocabulary (`mint` and `husk` both
appear in `mod.rs`'s merge-base doc). The workspace/tab/pane/split vocabulary predates
this diff in both the model and the schema. Comment voice is the repository's own
long-form argued style. Nothing reads as carried over from another implementation.

### references

Class and hit count only, per this directory's rule. Searched both gated files in full
at HEAD. Every term list was positive-controlled against a seeded scratch file outside
the repository before any count was trusted — each list hit its control — and the lists,
the seeds, and the reviewer's scratch copy of the diff were deleted after the run.

- terminal-multiplexer / terminal-tooling comparand class (10 terms, case-insensitive
  substring): 1 raw match, 0 comparand references — the common English display-surface
  noun in a pre-existing merge-base comment in `mod.rs`, outside this diff's hunks.
- registered `../SPECS.md` multiplexer/terminal names (4 terms): 0 hits. Neither file
  names a registered mux or terminal at all, so no protocol-vs-implementation
  disclosure is owed. This was checked deliberately hard: arrangement/layout is the
  territory where mux comparand framing would be easiest to smuggle.
- multiplexer layout-preset / layout-command vocabulary class (10 terms): 0 hits.
- capture-cited-by-path class (2 patterns): 0 hits. No capture is cited in this diff at
  all, by ID or otherwise.
- quarantined-artifact filename class (4 terms): 0 hits.

### leak

`./tools/leak-scan.sh` → `leak-scan: clean`, exit 0. `./tools/check-claims.sh` →
`check-claims: clean`, exit 0.

Verdict: PASS. Zero blockers. Both `none` declarations in this diff are true: the new
file is a pure first-party mapping — projection rows in, model out — whose every
non-obvious choice is argued from and verified against this repository's own schema and
model, and the sibling file's diff is a doc paragraph plus a module declaration that
leave its declaration standing. Two non-blocking notes are recorded above (the internal
ruling ID in a test comment, verified independently against the schema; the "older row"
ordering gloss the code does not rely on).
