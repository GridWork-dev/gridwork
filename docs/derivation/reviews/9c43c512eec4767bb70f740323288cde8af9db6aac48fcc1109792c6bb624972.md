subject: 9c43c512eec4767bb70f740323288cde8af9db6aac48fcc1109792c6bb624972
reviewer: fresh-context second reader — a reviewing lane with no exposure to any of the implementing sessions or their three prior review rounds
session: secondreader-adapter-trait-r4-2026-08-04
date: 2026-08-04

# Second-reader record

## Scope

Ten gated files across four gated prefixes: `crates/gwk-adapter-` (the claude,
codex and opencode adapter crates — three new `adapter.rs` modules, one edited
`event.rs`, three edited `lib.rs` crate docs) and `crates/gwk-parity/src/runners`
(all three engines' `lifecycle()`). The change introduces
`gwk_domain::engine::EngineAdapter` as gwk's own normalization trait, retracts
the previously-documented claim that the three adapters converge on the ACP SDK's
`Agent`, and converts the parity harness's axis-1 check from `&[String]` tags to
the typed `LifecycleFact`. Ungated files in the same change (`Cargo.toml`,
`crates/gwk-domain/**`, `crates/gwk-parity/src/checks.rs`, `docs/PARITY.md`,
`site/content/**`) were read for consistency but are outside the gate.

## Checks

- [x] citations — every non-obvious terminal behavior carries a `Derivation:` marker
      naming a permitted source, and the marker sits on the behavior it actually
      describes. `cleanroom-gate` already proved every cited ID exists in
      `docs/derivation/SPECS.md` or `docs/derivation/CAPTURES.md`; what it cannot check,
      and you can, is whether the citation is TRUE — that the code does what that source
      says, and came from it
- [x] framing — no source-derived framing in comments, names, or structure
- [x] references — no naming of other projects as comparands, and no capture cited by
      path: captures are referenced by their registered ID and SHA-256, never otherwise.
      Record the finding as the CLASS searched and the HIT COUNT — "terminal-tooling
      comparand class (5 terms): 0 hits" — never the query terms themselves. A record
      that lists them publishes the exact strings this check exists to keep out, and
      the record is the one artifact nobody re-reviews. Give the term count only if you
      ran the grep and know it — never reconstruct one. A name already registered in
      ../SPECS.md is a permitted source, not a query term: cite it, and say whether you
      checked the protocol or the implementation
- [x] leak — `./tools/leak-scan.sh` green

## Notes

**Gated set** was determined from `.github/cleanroom-paths.txt` rather than from the
briefing: `crates/gwk-adapter-` matched seven files, `crates/gwk-parity/src/runners`
matched three. `crates/gwk-parity/src/checks.rs` changed in the same commit and is NOT
gated — it is the crate's pure half, classified by its own line in
`.github/cleanroom-not-engine.txt`. Same for the new `crates/gwk-domain/src/engine.rs`.

**citations.** All ten gated files carry at least one marker. The three new `adapter.rs`
modules each carry `Derivation: none` with a reason, and each declaration was checked
rather than accepted: none spawns a process, opens a PTY, reads a byte, supervises a
session, or encodes terminal behavior. Every value each matches on is an
already-parsed sibling-module type (`CodexEvent`/`ThreadStatus`, `Event`/`SessionStatus`,
`Message`/`PreToolUseAsk`), and what each decides is which `docs/PARITY.md` axis entry
that value belongs to — a mapping onto this repository's own contract. No `none` sits
beside a real citation in any of the three; the spec IDs named in their module docs are
pointers to where the sibling markers live, explicitly disclaimed as citations for the
file itself.

`crates/gwk-adapter-codex/src/event.rs` was edited (four `const`s widened to
`pub(crate)`, one variant doc corrected). Both of its `CODEX-APP-SERVER` markers are
byte-identical to their pre-change text and still sit directly above the constants they
describe; the widening moved no marker and introduced no new cited behavior. The
corrected `TurnCompleted` doc now matches axis 1's codex row, which lists that
notification under error alone.

Marker truth was spot-checked where this change leans on it rather than assumed:
`crates/gwk-adapter-codex/src/adapter.rs:137` rests on the `Turn` marker's "populated
only when `status` is `failed`" and the marker says exactly that;
`crates/gwk-adapter-claude/src/adapter.rs:125` rests on `is_successful_result`'s
`CLAUDE-AGENT-SDK` marker and the subtype union there covers the arm the code takes.
The runners' own markers (`CODEX-APP-SERVER` × 4, `OPENCODE-SERVER` × 5,
`CLAUDE-HOOKS` × 1) are untouched by this change.

The retracted claim's replacement was checked against the artifact rather than believed:
the workspace's declared `agent-client-protocol` 2.0.0 defines `Agent` as a unit struct
implementing the SDK's `Role` trait with `type Counterpart = Client`. The claim now
written in `Cargo.toml`, three crate docs, `crates/gwk-domain/src/engine.rs` and
`docs/PARITY.md` is accurate, and `agent-client-protocol` appears in exactly one crate
manifest — `gwk-adapter-opencode`, the one that speaks the wire.

An independent sweep for any surviving site of the retracted claim found none: every
remaining occurrence in the tree sits inside text that explicitly retracts it.

**framing.** The vocabulary is this repository's — `EngineAdapter`, `LifecycleFact`,
`EngineStatus`, `EngineEvent` — and each variant traces to a column that already exists
in `docs/PARITY.md`'s axis tables. Nothing reads as transcribed. Worth recording as a
positive: `crates/gwk-domain/src/engine.rs` deliberately keeps `spawn_tui` off the trait
so the contract crate does not acquire a `gwk-pty` dependency, which under
`.github/cleanroom-paths.txt`'s direct-dependency rule would have pulled an ungated
crate under the gate. The reasoning is written at the site.

**references.** Terminal-tooling comparand class (23 terms): 0 hits across the whole
change. Captures cited by path: 0. Capture IDs cited at all: 0 — this change adds no
capture citation. One name needs saying rather than collapsing into that count: ACP is a
registered permitted source (`ACP-1` in `docs/derivation/SPECS.md`), and to verify the
retraction I read the SDK IMPLEMENTATION — the `agent-client-protocol` 2.0.0 crate
source this workspace already depends on, Apache-2.0 — not only the protocol page. That
is a permissively-licensed dependency's public API, which rule 2 does not reach.

**leak.** `./tools/leak-scan.sh` → `leak-scan: clean`, exit 0.

**Non-blocking observations**, recorded because they are real and none of them is an
escalation:

- `crates/gwk-parity/src/runners/codex.rs:262` states its own known limit honestly:
  `wait_for_response` discards notifications, so a `thread/started` arriving before the
  `thread/start` response is lost and the cell fails. Correct as stated, and confirmed at
  `codex.rs:86`. The residual is that the failure detail reads "missing lifecycle
  fact(s)", attributing to the engine a fact the harness dropped. Fixing it is a change
  to the drive loop, which this change correctly declines to make.
- `crates/gwk-adapter-opencode/src/adapter.rs:92,98` write two bus tag strings as
  literals. Unlike the codex sibling, `gwk-adapter-opencode`'s `event.rs` defines no
  named constants for them, so there is nothing to reuse — this is a drift risk, not the
  same defect that was fixed in codex.
- `crates/gwk-parity/src/runners/claude.rs:104` still explains the expected set in terms
  of what "this crate invents a tag for". Literally still true of `gwk-parity`, which
  invents no tags at all now, but the idiom is the one this change retired.
- `crates/gwk-adapter-codex/src/event.rs:33` widened `METHOD_THREAD_STARTED` to
  `pub(crate)` and nothing outside that module consumes it.

Supporting evidence: `cargo fmt --all --check` clean; `cargo clippy --workspace
--all-targets` no diagnostics; the five affected crates' tests pass (0 failed, live
engine cells `#[ignore]`d as before); the `curated-from` hash in
`site/content/docs/parity.mdx` and the `source-map.json` destination hash both match the
files they name.
