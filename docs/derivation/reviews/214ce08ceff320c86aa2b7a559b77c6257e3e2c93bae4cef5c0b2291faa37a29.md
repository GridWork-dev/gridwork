subject: 214ce08ceff320c86aa2b7a559b77c6257e3e2c93bae4cef5c0b2291faa37a29
reviewer: gw-general-purpose, fresh-context dispatch — no exposure to the implementing session (routed here per CLEANROOM.md rule 4's requirement for a fresh-context lane; every specialized GridWork agent carries house instructions that would be exactly the prior exposure the control exists to exclude) — round 2
date: 2026-08-01

# Second-reader record

## Scope

The codex engine adapter under `crates/gwk-adapter-codex/` — the JSON-RPC wire codec and child-process client (`src/wire.rs`), typed protocol bodies (`src/schema.rs`), notification/request normalization (`src/event.rs`), kernel-command mapping (`src/command.rs`), now 16 vendored app-server schemas with `PROVENANCE.md`, and the schema-freshness test; round 2 after the round-1 escalation on citation accuracy.

## Checks

- [x] citations — every non-obvious terminal behavior carries a `Derivation:` marker
      naming a permitted source, and the marker sits on the behavior it actually
      describes. `cleanroom-gate` already proved every cited ID exists in
      `docs/derivation/SPECS.md` or `docs/derivation/CAPTURES.md`; what it cannot check,
      and you can, is whether the citation is TRUE — that the code does what that source
      says, and came from it
- [x] framing — no source-derived framing in comments, names, or structure
- [x] references — no naming of other projects as comparands, and no capture cited by
      path: captures are referenced by their registered ID and SHA-256, never otherwise
- [x] leak — `./tools/leak-scan.sh` green

## Notes

**Verdict: PASS.** Round 1 (same base, prior subject) escalated on three
citation-accuracy findings; this round verifies the amended diff. The round-2 delta
against the content round 1 reviewed is exactly: the two newly vendored mapping
schemas (`schemas/ServerNotification.json`, `schemas/ServerRequest.json`) with their
`PROVENANCE.md` rows and `VENDORED_FILES` entries, and reworded markers/doc comments
in `src/event.rs` and `src/schema.rs`. `src/command.rs`, `src/wire.rs`, and all 14
previously vendored schemas are byte-identical to what round 1 verified (confirmed by
blob-level diff against the round-1 diff's hashes), so round 1's verification of those
stands: README verbatim quotes (`"jsonrpc":"2.0"` header omitted on the wire;
newline-delimited JSON (JSONL) over stdio; the two `generate-json-schema` quotes),
`docs/PARITY.md` quotes (version pin, "no currency exists anywhere in the protocol",
axis-2 `waitingOnApproval`, "two protocol generations in one schema bundle", the
public-CI language), and every shape spot-check (ThreadStatus/ThreadActiveFlag,
TurnStatus/TurnError, both approval pairs incl. snake_case amendment keys and the
absence of `availableDecisions` on file-change params, ThreadTokenUsage/
TokenUsageBreakdown incl. `cacheWriteInputTokens` default-0 and no currency field,
ServerRequestResolvedNotification, all ThreadItem variants and the six status enums).

### Round-1 findings, re-verified fixed

- **F1 (phantom `_server_notif_methods.txt`) — fixed.** Both markers now cite
  `schemas/ServerNotification.json`'s `oneOf`. Verified against the vendored file
  (itself parsed-JSON-identical to a fresh pinned-version generation, below): the
  `ErrorNotification` branch is exactly `method: ["error"]`, `params: {"$ref":
  "#/definitions/ErrorNotification"}`; the `Thread/startedNotification` branch is
  `method: ["thread/started"]`, `params: $ref ThreadStartedNotification` — the
  marker's parentheticals quote these pairs accurately, and all nine method-string
  constants in `event.rs` appear as method enums with `$ref`s to their matching
  params definitions. The fourth, implementer-found defect of the same class is also
  fixed: `ServerRequest.json` is now vendored, and the `event.rs` marker names its
  actual branch titles — verified as exactly
  `Item/commandExecution/requestApprovalRequest` (`method:
  ["item/commandExecution/requestApproval"]`, `params: $ref
  CommandExecutionRequestApprovalParams`) and `Item/fileChange/requestApprovalRequest`
  (`method: ["item/fileChange/requestApproval"]`, `params: $ref
  FileChangeRequestApprovalParams`). No path-shaped reference to a non-source
  artifact remains anywhere in the gated files — references now genuinely passes.
- **F2 (variant miscount) — fixed.** The `ThreadItem` doc now says eighteen variants,
  eight modeled and ten unmodeled, both lists enumerated; verified against the cited
  `v2/ItemCompletedNotification.json` `#/definitions/ThreadItem`: 18 `oneOf` branches,
  and both enumerations match the schema's tags exactly (modeled: `userMessage`,
  `agentMessage`, `reasoning`, `commandExecution`, `fileChange`, `mcpToolCall`,
  `collabAgentToolCall`, `subAgentActivity`; unmodeled: `hookPrompt`, `plan`,
  `dynamicToolCall`, `webSearch`, `imageView`, `sleep`, `imageGeneration`,
  `enteredReviewMode`, `exitedReviewMode`, `contextCompaction`).
- **F3 (`Turn.items` over-claim) — fixed.** The marker now quotes the schema's own
  description verbatim: "Thread items currently included in this turn payload." —
  which is character-for-character the `description` on `Turn.items` in
  `v2/TurnCompletedNotification.json`. The adjacent `Turn.error` paraphrase
  ("populated only when `status` is `failed`") remains faithful to the schema's
  "Only populated when the Turn's status is failed."

### Round-2 verification detail

- **Newly vendored files (probe path b):** `codex --version` → `codex-cli 0.146.0`
  (the pin). Parsed-JSON comparison of both `ServerNotification.json` and
  `ServerRequest.json` against the fresh `codex app-server generate-json-schema --out
  … --experimental` output: **2/2 MATCH** (round 1 already matched the other 14/14
  the same way). `schemas/` now holds exactly 16 `.json` files, matching the updated
  `VENDORED_FILES` list and the two new `PROVENANCE.md` rows, whose stated purposes
  ("the `method` ⇄ params-type mapping…") describe what the files actually contain.
- **Framing / references:** the reworded comments quote schema branch titles and
  structure only; no comparand project named, no capture cited by path, no
  transcription-smelling naming introduced.
- **Gates:** `cargo test -p gwk-adapter-codex --all-targets --locked --offline` — 42
  passed, 0 failed (40 unit + 2 freshness), with
  `vendored_schemas_match_a_fresh_generation` running as a live verification over all
  16 files against the pinned CLI. `./tools/leak-scan.sh` → "leak-scan: clean",
  exit 0. Subject recomputed via `git diff --no-renames --name-only c3e0c4a...HEAD |
  CLEANROOM_BASE=c3e0c4a ./tools/cleanroom-gate.sh --subject` and it matches this
  record's subject.
