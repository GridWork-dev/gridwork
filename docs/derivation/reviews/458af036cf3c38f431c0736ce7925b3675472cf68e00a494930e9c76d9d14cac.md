subject: 458af036cf3c38f431c0736ce7925b3675472cf68e00a494930e9c76d9d14cac
reviewer: fresh-context subagent dispatch, independent second-reader lane — no exposure to the implementing session; no SPEC, PLAN or kickoff document for this phase was read, and the diff was read cold from `git diff --no-renames main...HEAD`
session: secondreader-7p-e1-r1-2026-08-09
date: 2026-08-09

# Second-reader record

## Scope

Two new gated files, both under `crates/gwk-tui/src/workspace` and both declaring
`Derivation: none`. `mod.rs` is the mux object model: workspaces, tabs and split panes
as a pure weighted tree with create/navigate/resize/close operations and a
deterministic geometry solver that turns integer shares into cell rectangles.
`render.rs` paints that model's own furniture — pane borders, pane titles, the tab
strip, the workspace label, a status line — into a ratatui buffer and registers mouse
hit-regions for it. The same diff touches `crates/gwk-tui/src/lib.rs` (the `workspace`
module declaration plus a module-doc update), which is not a gated path; it was read
anyway and its description of what landed matches the two files.

## Checks

- [x] citations — every non-obvious terminal behavior carries a `Derivation:` marker
      naming a permitted source, and the marker sits on the behavior it actually
      describes. `cleanroom-gate` already proved every cited ID exists in
      `docs/derivation/SPECS.md` or `docs/derivation/CAPTURES.md`; what it cannot check,
      and you can, is whether the citation is TRUE — that the code does what that source
      says, and came from it
- [x] framing — no source-derived framing in comments, names, or structure
- [x] references — no naming of other projects as comparands, and no capture cited by
      path: captures are referenced by their registered ID and SHA-256, never otherwise.
      Record the finding as the CLASS searched and the HIT COUNT — never the query terms
      themselves. A name already registered in ../SPECS.md is a permitted source, not a
      query term: cite it, and say whether you checked the protocol or the implementation
- [x] leak — `./tools/leak-scan.sh` green

## Notes

Subject recomputed and matched: `git diff --no-renames --name-only main...HEAD |
./tools/cleanroom-gate.sh --subject` →
`458af036cf3c38f431c0736ce7925b3675472cf68e00a494930e9c76d9d14cac`, exit 0, against a
clean working tree at HEAD `3461a2d26fc4399f90e937c8d44807b9a3632427`. `--markers`
exits 0, reporting both gated files declare no derivation. The full gate's only
failure before this record existed was the record's own absence. No registry file is
edited by this diff, so no `CLEANROOM_BASE` row-diff applies.

### citations

Both files carry exactly one marker each, both `Derivation: none` — no `none` sits
beside a real citation. Each was checked as a claim, every line of both files read.

**`crates/gwk-tui/src/workspace/mod.rs:3-8` (`none`).** True. The file performs no I/O
of any kind: its imports are `std::cmp::Reverse`, `std::fmt`, and ratatui's `Rect`
used as plain geometry. No process is spawned (no process, command, or spawn construct
anywhere), no terminal byte is parsed or emitted, no session is supervised — panes are
structural leaves, and nothing references the kernel, a wire, or a process handle. No
colour/SGR fact (the file styles nothing) and no keyboard-protocol fact (the module
doc's "No bindings" is accurate: no key or event type appears). The declaration's
registry claim — "no registry row describes a mux object model" — was verified against
both registries: the mux-adjacent rows in `../SPECS.md` (`TMUX-MANUAL`,
`ZELLIJ-KEYBINDS`, `ZELLIJ-PRESETS` — registered permitted sources, named here as
citations, not comparands; the registry rows were read, not those projects'
implementations) register key-binding conventions and command/configuration
vocabulary only. No row describes an object model of workspaces, tabs and weighted
split trees — and the input vocabulary those rows do cover is exactly what this module
deliberately leaves out for a later element. The solver's one non-trivial claim
(rounded cumulative shares stay monotone, so rectangles tile the area exactly) is
arithmetic, not terminal behavior, and is proven in-file by the tiling test over an
odd-sized area. Nothing in the file demands a real citation, so the `none` is the
truthful form.

**`crates/gwk-tui/src/workspace/render.rs:3-8` (`none`).** True. Every cell write goes
through ratatui's `Buffer` API (`set_string`/`set_stringn`) — library-mediated
painting, the exemption CLEANROOM.md rule 2 records — and no escape sequence of this
crate's authorship exists in the file. The two border glyph sets are display
characters (box-drawing and an ASCII fallback) painted into buffer cells as data, not
protocol bytes. No process is spawned, nothing is parsed, nothing is supervised: pane
content is deliberately not painted, and the empty-surface and zero-area branches
return without touching anything. Styles all resolve through `ChromeTheme`
(`crate::chrome`, ungated — read to judge the claim) into the same `token_style` every
other surface uses, so no colour/SGR fact is asserted here; the tests compare resolved
foregrounds through that same path. Hit-regions are `HitMap` registrations
(`crate::input`, ungated — read likewise): rectangle geometry only, with the mouse
event types and their decoding living entirely in crossterm on the ungated side. The
one width claim — "byte length is cell width" for the header label and tab titles — is
scoped to minted ordinals (ASCII decimal strings from the model's own counters), where
it is trivially true; nothing else in the file measures text width.

### framing

No source-derived framing. The comments argue from invariants the module itself states
— weights are shares rather than cells, absence and invisibility are different facts,
a one-part split is a husk to collapse — and the names are generic domain nouns
(`Workspace`, `Tab`, `Split`, `Part`, `Axis`, `PaneId`, `WorkspaceTarget`). The
structure follows this crate's own seams (`chrome` for styling, `input` for
hit-testing) rather than any foreign shape: there is no imported command vocabulary,
no configuration-key names, no default-binding table — the model has no bindings at
all, and says why. The focus-move total order and the split/resize semantics read as
reasoned choices with their tie-breakers and no-op edges justified in place.

### references

Class and hit count only, per this directory's rule. Both gated files searched in
full (they are new, so full-file scope equals added-lines scope). Five term lists were
positive-controlled against a seeded scratch file outside the repository before any
zero was trusted (5 of 5 controls hit).

- terminal-multiplexer / session-manager comparand class (8 terms): 1 hit, false
  positive — the ordinary English display-surface word in a doc comment about
  directional focus ("in screen terms"), not a project name. 0 comparand uses.
- terminal-emulator / VT-implementation comparand class (10 terms): 0 hits.
- PTY-supervision-tool comparand class (7 terms): 55 hits, all the Rust `.expect(...)`
  assertion method in test code. 0 comparand uses.
- derivation-by-comparison phrasing class (9 terms): 0 hits.
- capture-cited-by-path class (2 patterns): 0 hits. Neither file cites any capture,
  by ID or otherwise, which is consistent with both declaring `none`.

Two registered `../SPECS.md` names appear in this record's citations section because
checking mod.rs's "no registry row describes a mux object model" claim required
reading their rows; the registry rows are what was checked, never those projects'
implementations, and neither name appears anywhere in the gated files.

### leak

`./tools/leak-scan.sh` → `leak-scan: clean`, exit 0. `./tools/check-claims.sh` →
`check-claims: clean`, exit 0.

Verdict: PASS. Zero blockers, zero findings. Both `none` declarations are true against
a full read of the files they describe: a pure in-memory object model with no I/O, and
a painter whose every byte reaches the terminal through a permissively-licensed
library's buffer. The gated boundary holds exactly where CLEANROOM.md draws it — the
wire half (session content in panes, detach/reattach routing) is not in this change,
and the module docs say so rather than gesturing past it.
