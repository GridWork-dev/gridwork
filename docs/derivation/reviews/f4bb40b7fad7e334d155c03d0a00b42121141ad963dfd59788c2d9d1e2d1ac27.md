subject: f4bb40b7fad7e334d155c03d0a00b42121141ad963dfd59788c2d9d1e2d1ac27
reviewer: fresh-context subagent dispatch, independent second-reader lane — no exposure to the implementing session; no SPEC, PLAN or kickoff document for this phase was read, and the diff was read cold from `git diff --no-renames main...HEAD`
session: secondreader-replay-rest-r1-2026-08-08
date: 2026-08-08

# Second-reader record

## Scope

Two gated paths, one marker-owing, plus a registry row change the gate folds into this
subject because a gated file cites it. `crates/gwk-pty/src/session.rs` rewords the
`POSIX-TERM §11.1.10` marker on `kill()`: its parenthetical enumeration of the
chapter's other pseudo-terminal mentions grows from two sections to three, adding
`§11.2.1`'s `O_TTY_INIT` parenthetical alongside `§11.1.1`'s open path and `§11.2.4`'s
control-mode mentions. `crates/gwk-pty-host/gwk-pty-host.service` replaces an uncited,
generally-stated empirical claim about `PrivateDevices=yes` refusing PTY allocation
with a `Derivation: CAP-005` citation. `docs/derivation/CAPTURES.md` registers `CAP-005`
(row plus its own-observation-clause paragraph) and `docs/derivation/captures/
private-devices-pty-open.txt` is the new capture file the row binds to. Both changes
read as the direct, named fix for the two non-blocking findings of the prior second-
reader record on this crate pair (subject `120a78bd...`): finding 1 asked for exactly
this citation-or-capture fix on the `.service` file, and finding 2 asked for exactly
this one-section completion of the `kill()` marker's enumeration.

## Checks

- [x] citations — every non-obvious terminal behavior carries a `Derivation:` marker
      naming a permitted source, and the marker sits on the behavior it actually
      describes. `cleanroom-gate` already proved every cited ID exists in
      `docs/derivation/SPECS.md` or `docs/derivation/CAPTURES.md`; what it cannot check,
      and you can, is whether the citation is TRUE — that the code does what that source
      says, and came from it
- [x] framing — no source-derived framing in comments, names, or structure
- [x] references — no naming of other projects as comparands, and no capture cited by
      path: captures are referenced by their registered ID and SHA-256, never otherwise.
      Record the finding as the CLASS searched and the HIT COUNT — never the query terms
      themselves. A name already registered in ../SPECS.md is a permitted source, not a
      query term: cite it, and say whether you checked the protocol or the implementation
- [x] leak — `./tools/leak-scan.sh` green

## Notes

Subject recomputed and matched: `git diff --no-renames --name-only main...HEAD |
CLEANROOM_BASE="$(git merge-base main HEAD)" ./tools/cleanroom-gate.sh --subject` →
`f4bb40b7fad7e334d155c03d0a00b42121141ad963dfd59788c2d9d1e2d1ac27`, against a clean
working tree at HEAD `eb281f24a2f51c965790a47fa1a4d97b4225760b`. The gate's own note —
`CAPTURES.md row 'CAP-005' changed ... crates/gwk-pty-host/gwk-pty-host.service cites
it, treating it as touched` — is exactly the mechanism `docs/derivation/reviews/
README.md` describes: an uncited-then-cited row re-opening the citing file's review.
`--markers` exits 0 over the one marker-owing `.rs` file (`ECMA-48 CAP-002
POSIX-WINSIZE POSIX-WINSIZE POSIX-TERM`); the full gate's only failure before this
record existed was the record's own absence. `./tools/check-claims.sh` → clean.

### citations

**`crates/gwk-pty/src/session.rs:229-241` (`POSIX-TERM §11.1.10`, reworded).** Fetched
`pubs.opengroup.org/onlinepubs/9799919799/basedefs/V1_chap11.html` (the registered URL)
directly, three times, narrowing each time. §11.1.10's quoted sentence is verbatim:
"the SIGHUP signal shall be sent to the controlling process for which the terminal is
the controlling terminal" — the marker's "one process, not a process group and not the
session" reading is correct against that noun phrase. Then an exhaustive substring
search of the whole chapter page for "pseudo-terminal" returned exactly three
locations, in this order: §11.1.1 ("Opening the subsidiary side of a pseudo-terminal"),
§11.2.1 (the `O_TTY_INIT` sentence's "other than a pseudo-terminal" exclusion), §11.2.4
(two separate sentences — the non-default-value-support caveat and the baud-rate
caveat). That is exactly the marker's new enumeration — three sections, §11.2.4 credited
with two mentions — and none of the three says anything about a master-side close
presenting a disconnect to the slave side, which is the marker's negative claim. The
rewording is a precise, checked fix of the prior record's finding 2 (the enumeration
was one section short, missing `§11.2.1`), not a reword that traded one imprecision for
another. `CAP-002`, cited in the same marker as "the mirror direction of the same
pairing," is unchanged by this diff and was not re-verified against its own probe this
round; its row text was read and is consistent with what the marker asks it to carry.

**`crates/gwk-pty-host/gwk-pty-host.service:73-76` (`Derivation: CAP-005`, new).** The
capture file `docs/derivation/captures/private-devices-pty-open.txt` hashes to
`953f5e70c2eab37ec803fb7576ac6a05c20cc5f7f5389ce9595b5a794572b258` (recomputed with
`sha256sum`), matching the `CAP-005` row byte for byte. The row text and the capture
file agree: device paths present, both `posix_openpt(3)` and `open("/dev/ptmx")` fail
`ENOENT` under `PrivateDevices=yes`, both succeed in the control unit. The service
file's citation — "exposed all three PTY device paths while both `posix_openpt(3)` and
`open("/dev/ptmx")` failed ENOENT; the control unit was clean" — is an accurate,
narrower restatement of the same two facts, not an oversell. This is the named fix for
the prior record's finding 1, which flagged the old prose's uncited general claim and
suggested exactly this: "reword to what was observed on this deployment, or register
the probe as a capture and cite the ID in prose." The registered `CAP-005` paragraph in
`CAPTURES.md` states the same scope discipline the row needs ("does not generalize
that result to every systemd or kernel version"), and the capture file's own probe
script and both `systemd-run` invocations are present and re-runnable, satisfying the
own-observation clause the same way `CAP-002`/`CAP-003`/`CAP-004` do. `CAP-005` is
sequential after `CAP-004`, not reused.

### framing

No source-derived framing. The `.service` comment separates the cited fact (what
`CAP-005` observed) from the implementation's own choice (removing `PrivateDevices`)
with its own bullet, worded as a consequence ("engine children need the PTY allocation
path the captured unit refused"), not as a spec quote. The `session.rs` marker's
enumeration reads as this crate's own accounting of what the chapter does and does not
say, not as text carried in from elsewhere. Names are unchanged by this diff.

### references

Class and hit count only, per this directory's rule. Searched the two gated files plus
the two touched registry/capture files (`docs/derivation/CAPTURES.md`,
`docs/derivation/captures/private-devices-pty-open.txt`) — small footprint, so full-file
rather than added-lines-only. Five term lists were positive-controlled against a seeded
scratch file before any zero was trusted (5 of 5 controls hit).

- terminal-multiplexer / session-manager comparand class: 0 hits.
- terminal-emulator / VT-implementation comparand class: 2 hits, both false-positive —
  the pinned in-repo engine library name (already named at the merge base, a permitted
  build dependency) and a terminfo database entry set as the unit's `TERM` deployment
  default, with an in-line comment already saying that's what it is. 0 comparand uses.
- PTY-supervision-tool comparand class: 20 hits, all the Rust `.expect(...)` assertion
  method in test code. 0 comparand uses.
- derivation-by-comparison phrasing class ("same as X", "mirrors X", "based on X",
  "copied from", etc.): 0 hits.
- capture-cited-by-path class (a capture named by its file path outside the registry
  table): 0 hits. `CAP-005` is cited by registered ID only, in both the `.service` file
  and the `CAPTURES.md` prose paragraph.

No registered `../SPECS.md` name is newly cited by this diff, so no protocol-vs-
implementation disclosure applies beyond what the prior record already recorded for
`POSIX-TERM`.

### leak

`./tools/leak-scan.sh` → `leak-scan: clean`, exit 0. `./tools/check-claims.sh` →
`check-claims: clean`, exit 0.

Verdict: PASS. Zero findings. Both changes are exactly the fixes the prior second-
reader record (`120a78bd...`) named — the `CAP-005` capture-and-citation for finding 1,
the completed three-section enumeration for finding 2 — and both check out against
their sources rather than against the finding text that asked for them: the marker's
new enumeration matches an exhaustive fetch of the chapter, and the new capture's hash,
row text and citing prose all agree with each other and with the probe output.
