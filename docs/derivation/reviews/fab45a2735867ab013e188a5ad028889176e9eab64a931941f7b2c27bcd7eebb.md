subject: fab45a2735867ab013e188a5ad028889176e9eab64a931941f7b2c27bcd7eebb
reviewer: fresh-context subagent dispatch, independent second-reader lane — no exposure to the implementing session; no SPEC, PLAN or kickoff document for this phase was read, and the diff was read cold from `git diff --no-renames main...HEAD`
session: secondreader-7p-p16-r2-2026-08-09
date: 2026-08-09

# Second-reader record

## Scope

Five gated paths, the interned wire shape adopted end to end. `crates/gwk-pty-host/src/wire.rs`
serves snapshots as `PtyFrame::from_cells` (a first-appearance style table plus per-row runs)
and builds each delta batch its own batch-scoped style table through `StyleInterner`, interning
only cells that actually changed; it gains four size-floor tests pinning the measured
geometries (blank 240x70 in kilobytes, eight blank 80x24 inside one publish budget, the
attr-heavy fixture at an exact re-measured byte count, and the all-distinct-styles worst case
still refused by actual bytes). `crates/gwk-pty-host/src/publish.rs` re-derives its
declaration-time `blank_grid_bytes` to the interned arithmetic — one measured blank style, one
measured fill run per row, fixed punctuation — making the blank form O(rows), with tests
pinning that 240x70 (and sixteen of them) now declare while a far-edge geometry still refuses.
`crates/gwk-pty-host/src/session.rs` widens one match pattern to the two-field
`CellsChanged`. `crates/gwk-pty-host/src/registry.rs` (tests only) resolves updates through
the batch's style table, reads snapshots through the `cells()` expansion, and gains a
quiescence fix in the detach/reattach test's echo wait. `crates/gwk-pty-host/tests/detach.rs`
adapts its row-text helper to the same `cells()` expansion; its three markers are untouched.
No registry row is touched.

## Checks

- [x] citations — every non-obvious terminal behavior carries a `Derivation:` marker
      naming a permitted source, and the marker sits on the behavior it actually
      describes. `cleanroom-gate` already proved every cited ID exists in
      `docs/derivation/SPECS.md` or `docs/derivation/CAPTURES.md`; what it cannot check,
      and you can, is whether the citation is TRUE — that the code does what that source
      says, and came from it
- [x] framing — no source-derived framing in comments, names, or structure
- [x] references — no naming of other projects as comparands, and no capture cited by
      path: captures are referenced by their registered ID and SHA-256, never otherwise.
      Record the finding as the CLASS searched and the HIT COUNT — never the query terms
      themselves. A name already registered in ../SPECS.md is a permitted source, not a
      query term: cite it, and say whether you checked the protocol or the implementation
- [x] leak — `./tools/leak-scan.sh` green

## Notes

Subject recomputed and matched: `git diff --no-renames --name-only main...HEAD |
./tools/cleanroom-gate.sh --subject` →
`fab45a2735867ab013e188a5ad028889176e9eab64a931941f7b2c27bcd7eebb`, against a clean
working tree at HEAD `b2cfe8847ff93d3bd92328d9026546c871e5ea9f`. `--markers` over the
same file list exits 0 (`publish.rs`, `registry.rs`, `session.rs`, and `wire.rs` declare
no derivation; `detach.rs` cites `POSIX-TERM` three times).

### citations

**`crates/gwk-pty-host/src/wire.rs` (`Derivation: none` — the file that gained real
encoding logic, so the declaration was re-decided from the code as it now stands, not
carried forward).** The declaration reads *"converts the engine crate's own typed output
into the wire contract's types field by field; no terminal-protocol byte is parsed and no
process is supervised here."* Every new construct was checked against that claim:

- The interning itself — `snapshot()` via `PtyFrame::from_cells`, the per-batch
  `StyleInterner` in `apply`, the intern-on-change in `set_cell` — is original wire-format
  design over this repository's own types (`gwk-domain`'s frame module, whose own `none`
  declaration calls it "original wire encoding over this repository's own existing style
  type"). Nothing here parses a byte, supervises a process, or asserts a colour/SGR/keyboard
  fact. The doc claims were verified against the code they name: *"a first-appearance style
  table, maximal same-style runs per row (the `from_cells` canonical form)"* — TRUE against
  `gwk-domain`'s `from_cells` (first-appearance `HashMap` interner, maximal same-style scan,
  uniform-glyph runs as fills); *"Only a CHANGED cell interns its style, so the batch table
  carries exactly the styles its updates reference"* — TRUE (the equality early-return in
  `set_cell` precedes the intern); *"every message self-contained, no index space outliving
  it"* — TRUE (a fresh `StyleInterner::default()` per `apply` call, moved into the batch by
  `into_styles`).
- The new attr-heavy size test authors escape-byte fixtures and feeds them to the crate's own
  pinned VT engine. This is the file's established fixture-not-a-claim discipline — stated in
  the merge-base test `styles_ride_the_conversion`: *"The escape bytes are a fixture, not a
  claim"*, with the SGR semantics "pinned, with its citations, in `crates/gwk-pty/tests/style.rs`"
  — and that citation home was verified at HEAD: the engine crate's style tests carry
  `ECMA-48 §8.3.117`, `XTERM-CTLSEQS`, and `KITTY-UNDERLINES` markers for the attribute,
  palette, truecolor, and underline-color facts the fixture exercises. The deciding point: no
  assertion in any new test depends on what an escape sequence MEANS. The pinned values
  (3,455 bytes; 2,716,431 bytes) are measured outputs of the repository's own serializer,
  asserted exactly so a drift is looked at; the worst-legal-case test builds its styles
  directly on the mirror with no escape bytes at all. The `none` declaration REMAINS true.

**`crates/gwk-pty-host/src/publish.rs` (`Derivation: none`).** The rewritten
`blank_grid_bytes` computes serialized sizes of the repository's own JSON wire types — no
terminal byte, no process, exactly as the declaration says. Its load-bearing claims were
each verified: *"by the same arithmetic as the kernel hub's own `blank_grid_bytes` under
the interned shape"* — TRUE: `crates/gwk-kernel/src/wire/pty.rs`'s `blank_grid_bytes`
computes the identical `(style + 22) + rows x (fill + 3)` from the same two measured
serializations, and its doc names the host's check as its mirror. *"22 punctuation bytes
outside the per-row term"* — TRUE, re-derived independently from the serialized
`PtyFrame` shape (`{"styles":[` = 11, the closing `]` = 1, `,"rows":[` = 9, `]}` = 2,
total 23, minus the last row's absent joining comma folded into the per-row `+3` term);
the field order and exact key set are pinned by `gwk-domain`'s own
`pty_frame_dimensions_are_derived_not_stored` test. *"`parse_sessions` refused zero
dimensions before this is ever asked"* — TRUE (the empty-name/engine/size refusal
precedes the budget check). The `none` REMAINS true.

**`crates/gwk-pty-host/src/registry.rs` (`Derivation: none`, test-only diff).** The
declaration ("a map of task handles and channel plumbing") is unchanged and the non-test
code is untouched. The one piece of reasoning this diff adds — the quiescence comment on
the detach/reattach test: the child *"echoes the line (row 0) AND writes its own copy
(row 1)"* and sending the next line while the copy is in flight lets the orderings
interleave — was the question this review was asked to decide, and the answer is:
**observed first-party behavior of the test's own child process, not a terminal-protocol
fact needing a citation.** The comment asserts nothing about WHY the line appears twice
and cites no protocol semantics; it records that two writers race into one stream — the
test's own child's observed output layout, used only to sequence the test's inputs, in
the same voice as the merge-base's existing coalescing comment in the eviction test. The
`none` REMAINS true.

**`crates/gwk-pty-host/src/session.rs` (`Derivation: none`).** The diff is one widened
match pattern (`CellsChanged { updates, .. }`) adapting `has_content` to the two-field
variant — no new behavior, no new fact. The declaration (supervision over the engine
crate's own typed API; no terminal-protocol byte parsed here) REMAINS true.

**`crates/gwk-pty-host/tests/detach.rs` (three `POSIX-TERM` markers).** The diff touches
only the `rows_text` helper (reading the frame through the `cells()` expansion instead of
a direct field). All three markers were re-checked in place: §11.1.3 still sits on the
module doc governing the whole-file behavior, §11.1.11 still sits directly on the
`ACK:pending` await it licenses, and §11.1.10 still sits directly on the two absence
assertions (`GOT-HUP`, `SAW-EOF`). The quoted fragments and the "this crate's reading"
inference labels are byte-identical to the form a prior record in this directory verified
against the fetched specification; nothing moved off its behavior.

### framing

First-principles throughout. The new tests are measurement-backward — each pins a byte
count the repository's own spike measured through its own shipped path, with the pinned
numbers dated and the reason for exactness stated — which is provenance-forward test
design, not transcription. Names (`StyleInterner`, `PtyRun::Fill`,
`a_blank_240x70_frame_serializes_in_kilobytes_not_megabytes`,
`the_b4_floor_of_sixteen_declared_sessions_all_seed_at_declaration`) are plain
descriptions; comments cite the repository's own spike measurements and internal element
labels. Nothing reads as carried over from another implementation.

### references

Class and hit count only, per this directory's rule. Searched all five gated files in
full at HEAD. The comparand term list was positive-controlled against a seeded scratch
file outside the repository before any count was trusted — 2 of 2 control hits — and the
scratch file was deleted after the run.

- terminal-multiplexer / terminal-tooling comparand class (10 terms, case-insensitive
  substring): 93 raw matches, 0 comparand references. 92 are the common English
  display-surface noun and the crate's own screen-mirror identifier family (5 in
  `publish.rs`, 6 in `registry.rs`, 16 in `session.rs`, 62 in `wire.rs`, 3 in
  `detach.rs`); 1 is the crate's openly-declared pinned VT engine dependency name inside
  a pre-existing merge-base comment in `registry.rs`, outside this diff's hunks — a
  permitted declared dependency, not a comparand.
- registered `../SPECS.md` multiplexer/terminal names (4 terms): 6 hits, all in
  `wire.rs`, all the repository's own `Xterm256` wire-type identifier (and its test
  name). These reference the registered `XTERM-CTLSEQS` PROTOCOL — its 256-color palette
  indexing, which the wire vocabulary names — not the implementation; no implementation
  behavior is referenced or compared anywhere in the diff.
- capture-cited-by-path class (2 patterns): 0 hits. No capture is cited in this diff at
  all, by ID or otherwise.

### leak

`./tools/leak-scan.sh` → `leak-scan: clean`, exit 0. `./tools/check-claims.sh` →
`check-claims: clean`, exit 0.

Verdict: PASS. Zero blockers. The four `none` declarations all remain true as the files
now stand — the one that gained real logic (`wire.rs`) gained only first-party wire-format
encoding plus fixture-driven size measurements whose semantic claims live, cited, in the
engine crate's own tests; the echo-ordering reasoning in `registry.rs` is observed
first-party child behavior, not an uncited protocol fact; and `detach.rs`'s three
`POSIX-TERM` markers still sit exactly on the behaviors they license.
