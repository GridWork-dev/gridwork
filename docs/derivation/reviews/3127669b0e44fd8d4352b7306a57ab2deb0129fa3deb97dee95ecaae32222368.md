subject: 3127669b0e44fd8d4352b7306a57ab2deb0129fa3deb97dee95ecaae32222368
reviewer: fresh-context subagent dispatch, independent second-reader lane — no exposure to the implementing session; no SPEC, PLAN or kickoff document for this phase was read, and the stacked diff was read cold from `git diff --no-renames feat/7p-mux-pane-lifecycle...HEAD`
session: secondreader-7p-e5-r1-2026-08-09
date: 2026-08-09

# Second-reader record

## Scope

Two gated paths, a scrollback read path. `crates/gwk-pty/src/lib.rs` gains
`Grid::total_rows` and `Grid::screen_rows_text` (a selection-restricted plain-text
formatter read over absolute screen rows), reroutes the existing `format()` through a
shared `run_formatter` so the whole-screen and selection-restricted paths share the
sizing-quirk handling, and corrects `text()`'s doc comment (it claimed "the active
screen"; the formatter renders every retained row — behavior untouched, comment
corrected). `crates/gwk-pty/src/scroll.rs` is new: `HistoryView`, one viewer's
offset-from-live scroll position with clamping, declared `Derivation: none`. No
registry row is touched and no new citation is introduced.

## Checks

- [x] citations — every non-obvious terminal behavior carries a `Derivation:` marker
      naming a permitted source, and the marker sits on the behavior it actually
      describes. `cleanroom-gate` already proved every cited ID exists in
      `docs/derivation/SPECS.md` or `docs/derivation/CAPTURES.md`; what it cannot check,
      and you can, is whether the citation is TRUE — that the code does what that source
      says, and came from it
- [x] framing — no source-derived framing in comments, names, or structure
- [x] references — no naming of other projects as comparands, and no capture cited by
      path: captures are referenced by their registered ID and SHA-256, never otherwise.
      Record the finding as the CLASS searched and the HIT COUNT — never the query terms
      themselves. A name already registered in ../SPECS.md is a permitted source, not a
      query term: cite it, and say whether you checked the protocol or the implementation
- [x] leak — `./tools/leak-scan.sh` green

## Notes

Subject recomputed and matched: `git diff --no-renames --name-only
feat/7p-mux-pane-lifecycle...HEAD | ./tools/cleanroom-gate.sh --subject` →
`3127669b0e44fd8d4352b7306a57ab2deb0129fa3deb97dee95ecaae32222368`, against a clean
working tree at HEAD `b97fa961a0a8d8b5e80312b1ad5b9c985c8aa5e5`. `--markers` over the
same file list exits 0 (`lib.rs` cites `ECMA-48` twice; `scroll.rs` declares no
derivation).

### citations

**`crates/gwk-pty/src/scroll.rs` (`Derivation: none`, new file — the claim checked
hardest, as this directory's README requires).** All 194 lines read. The file is pure
offset arithmetic over this crate's own `Grid` API: `saturating_add`/`saturating_sub`
and a `min` clamp against `Grid::scrollback_rows`, with reads through
`Grid::screen_rows_text`. It spawns no process, parses no byte, supervises no session,
authors no escape sequence, maps no key, and asserts no wire-protocol or
control-function fact. Its doc comments assert only properties of this crate's own API
— that eviction can shrink history under a held view, that a clamped read is preferable
to a refusal — and both the offset-from-live model and the clamp are argued in-file
from first principles (an absolute row number would silently rename lines under new
output; a refusal would pin the caller to a position that cannot be read), which is
design reasoning, not spec restatement. Its tests drive the grid with plain text and
CR/LF line endings only — the same already-established obvious usage the crate's
pre-existing tests use — and assert positions of this crate's own rows, no new
control-function fact. The `none` sits alone in the file, beside no real citation, as
rule 3 requires. The declaration is TRUE.

**`crates/gwk-pty/src/lib.rs` (no new markers — verified none are owed).** The added
text asserts no new external-behavior fact: `total_rows` and `screen_rows_text` are
documented as reads over this crate's own screen model, and their implementation is
dependency-API use of the crate's openly-declared, pinned VT library (`Selection`,
`Point::Screen`, `PointCoordinate`, `FormatterOptions::with_selection`) — using a
permitted dependency's public types, not encoding anyone's terminal behavior. The
`screen_rows_text` doc's formatting claims (no trimming, no unwrapping, one screen row
per output line) are statements of this crate's own output contract, explicitly pinned
to match `text()`. The `text()` doc correction is framed entirely as a statement about
this crate's own method's history ("a property this method never had: the formatter
renders the whole screen") — no standard and no other implementation is invoked. The
`run_formatter` refactor moves no marker and changes no cited behavior: the two
pre-existing `ECMA-48` markers (§8.3.21 on the trailing CUP emission in `snapshot_vt`,
lib.rs:257; §8.3.20 on the cursor-forward test, lib.rs:281) fall outside every hunk of
this diff — byte-untouched — and each still sits directly on the behavior it describes:
the CUP marker on the 1-based `\x1b[{row};{col}H` emission, the CUF marker on the
advance-by-Pn-default-1 assertions, both consistent with what those ECMA-48 sections
define. Nothing anywhere in the diff asserts a colour/SGR fact, a keyboard-protocol
fact, or what unit any winsize field carries.

### framing

First-principles throughout. The names (`HistoryView`, `offset`, `is_live`,
`scroll_up`/`scroll_down`, `to_top`/`to_live`, `screen_rows_text`, `total_rows`,
`run_formatter`) are plain descriptions of this crate's own model, and the comment
voice — long-form rationale arguing consequences from the crate's own API constraints,
including the self-correcting note on `text()` — matches the established voice of the
merge-base file exactly. `HistoryView`'s structure is a single clamped offset, about
as minimal as the concept admits; nothing in names, comments, or structure reads as
transcribed from another implementation.

### references

Class and hit count only, per this directory's rule. Searched the full 372-line stacked
diff (both gated files' changed content plus context). All five term lists were
positive-controlled against a seeded scratch file outside the repo before any zero was
trusted — 5 of 5 control hits — and the scratch file and term lists were deleted after
the run.

- terminal-multiplexer / session-manager comparand class (7 terms): 0 hits.
- terminal-emulator / VT-implementation comparand class (9 terms): 0 hits. The crate's
  pinned VT dependency, named at the merge base and throughout the crate, is a
  permitted declared dependency and was deliberately outside this class; this diff's
  uses of it are import lines and public-API type names.
- PTY-supervision-tool comparand class (5 terms): 0 hits.
- derivation-by-comparison phrasing class (8 patterns): 0 hits.
- capture-cited-by-path class (3 patterns): 0 hits. No capture is cited in this diff
  at all, by ID or otherwise.

No registered `../SPECS.md` name is newly cited by this diff — the only citations in
the touched files are the two pre-existing `ECMA-48` section markers, untouched — so no
protocol-vs-implementation disclosure is owed beyond prior records on this file.

### leak

`./tools/leak-scan.sh` → `leak-scan: clean`, exit 0. `./tools/check-claims.sh` →
`check-claims: clean`, exit 0.

Verdict: PASS. Zero findings. The one marker this diff adds is a `none` declaration,
and it is truthful: the new module is a view-position model over this crate's own grid
API, deriving nothing. The one behavioral-looking edit — the `text()` doc change — is a
correction of this crate's description of itself, with behavior untouched, and the
refactor beneath it leaves both pre-existing citations exactly where they were, on
exactly the behaviors they describe.
