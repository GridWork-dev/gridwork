subject: e8e63c7f16d972d0d22c5dcd4d0497460c1e909af6b0c7522f4b3f0ac4ce9585
reviewer: fresh-context subagent dispatch, independent second-reader lane — no exposure to the implementing session; no SPEC, PLAN or kickoff document for this phase was read, and the diff was read cold from `git diff --no-renames be3de11...HEAD`
session: secondreader-pty-attach-r1-2026-08-07
date: 2026-08-07

# Second-reader record

## Scope

Twelve gated paths, eleven marker-owing. This is the attach hookup: the host's half of
the PTY wire family crossing the socket for the first time. Under `crates/gwk-pty-host/`:
`src/publish.rs` (new — one forwarding task per declared session: a fresh local attach
seeds a `pty_publish_snapshot`, live batches ride `pty_publish_deltas`, any break in
continuity reconnects and reseeds, a `validation` refusal alone is fatal; plus the
`GWK_PTY_HOST_SESSIONS` declaration parser, refused whole on the first bad entry);
`src/main.rs` (starts every declared session through the registry, spawns the publishers,
orders shutdown sessions-first so the last act is a typed retire); `src/kernel_client.rs`
(the three publish verbs as typed calls, a `Refused` error, and the response loop now
skipping the two PTY stream frames); `src/registry.rs` (a standalone `Attacher` handle so
a publisher's attach never holds the shared registry lock); `src/lib.rs` (module docs
record the boundary move); the systemd unit (three hardening lines relaxed for engine
children, with rationale, plus `TERM`); `src/envelope.rs`, `src/ingest.rs`,
`src/logging.rs` (a stale path pointer fixed at five sites); `src/wire.rs` (test prose
marking an escape-byte fixture as pinned elsewhere). Under `crates/gwk-pty/`:
`src/session.rs` gains a `POSIX-TERM` marker on `kill()`; `tests/style.rs` rewords the
`KITTY-UNDERLINES`/`XTERM-CTLSEQS` marker pair on the attribute-pile test. The kernel-side
half of the same change — the session hub `crates/gwk-kernel/src/wire/pty.rs`, the serve
wiring, three new requests and two new results in `gwk-domain`, goldens and bindings —
is outside the gated set and was read for context. Neither registry file is touched, so
the gate needed no `CLEANROOM_BASE` to compute the subject.

## Checks

- [x] citations — every non-obvious terminal behavior carries a `Derivation:` marker
      naming a permitted source, and the marker sits on the behavior it actually
      describes. `cleanroom-gate` already proved every cited ID exists in
      `docs/derivation/SPECS.md` or `docs/derivation/CAPTURES.md`; what it cannot check,
      and you can, is whether the citation is TRUE — that the code does what that source
      says, and came from it
- [x] framing — no source-derived framing in comments, names, or structure
- [x] references — no naming of other projects as comparands, and no capture cited by
      path: captures are referenced by their registered ID and SHA-256, never otherwise.
      Record the finding as the CLASS searched and the HIT COUNT — never the query terms
      themselves. A name already registered in ../SPECS.md is a permitted source, not a
      query term: cite it, and say whether you checked the protocol or the implementation
- [x] leak — `./tools/leak-scan.sh` green

## Notes

Subject recomputed and matched:
`git diff --no-renames --name-only be3de11...HEAD | ./tools/cleanroom-gate.sh --subject`
→ `e8e63c7f16d972d0d22c5dcd4d0497460c1e909af6b0c7522f4b3f0ac4ce9585`, against a clean
working tree. `--markers` reports the eleven `.rs` files — nine host files declaring no
derivation, two `gwk-pty` files citing — and exits 0. No file pairs a `none` with a real
citation. The systemd unit owes no marker by the gate's own file-class rule. Per the
standing methodology note, every command feeding a count or a redirect in this review was
run through the shell-filter bypass; the gated-diff corpus is 1016 lines, consistent with
the diffstat's gated share.

### citations

The three marker sites this diff adds or rewords were each verified against the source
fetched at its registered location this round; the two POSIX-WINSIZE markers riding
unchanged in the touched `session.rs` were re-verified against the function page the same
way, since the whole file is in-subject.

- **`crates/gwk-pty/src/session.rs:229-239`, the new `POSIX-TERM §11.1.10` marker on
  `kill()`.** The quoted clause is verbatim in the chapter: "the SIGHUP signal shall be
  sent to the controlling process for which the terminal is the controlling terminal",
  conditioned exactly as the marker says on a modem disconnect with CLOCAL unset. The
  marker's "one process, not a process group and not the session" is the chapter's own
  word — and notably it does NOT repeat the foreground-process-group characterization the
  previous record's aside used, which the chapter text does not support; the marker went
  to the source, not to the reviewer's paraphrase. The honest split is exactly right:
  master-close-presents-a-disconnect is declared "this crate's reading of the PTY
  pairing, not a sentence the chapter contains", and I confirmed the chapter contains no
  such sentence. The marker sits on the behavior it explains — `kill()` exists because
  the hangup path reaches one process at most, and `child.kill()` does not ride it. This
  discharges the previous record's finding 1, avoiding the chapter-row-vs-function-row
  trap `SPECS.md`'s header warns about: §11.1.10's disconnect rule is chapter text, and
  the function facts stay on `POSIX-WINSIZE`.
- **`crates/gwk-pty/tests/style.rs:269-270`, `KITTY-UNDERLINES` reworded.** The page
  states that 58 sets the underline color and "works exactly like the codes `38, 48`" —
  the marker now claims exactly that and no colon form, discharging the previous
  record's finding 3.
- **`crates/gwk-pty/tests/style.rs:271-276`, `XTERM-CTLSEQS` reworded.** The page carries
  `38:2:Pi:Pr:Pg:Pb` with "Set foreground color using RGB values" and, verbatim, "The
  color space identifier Pi is ignored." The marker now says ignored — not may-be-empty —
  and pins the empty-slot shape on the assertion against the engine, not the page.
  Discharges the previous record's finding 2.
- **`POSIX-WINSIZE`, both markers in `session.rs` (unchanged lines, in-subject).** "No
  signal shall be delivered if the terminal size was changed to the same value it had
  before the tcsetwinsize() call" and "Otherwise, -1 shall be returned, the terminal
  window size shall not be changed" are both verbatim on the function page, and the
  ponytail note's errno list matches the page's ERRORS section term for term.
- **The remaining markers in the two `gwk-pty` files** ride unchanged lines whose claims
  the previous record verified against each source at its registered location one day
  ago; the file text at those sites is character-identical to what that verification
  bound, and I re-read each in place against its registry row rather than refetching
  every document.

**The nine `Derivation: none` declarations, each read as a claim** — two of them changed
or new, and those two got the deeper read the README demands.

- **`main.rs`** now starts and stops sessions, which is the fact that makes its amended
  declaration worth checking hardest. What the file actually does: parses the operator's
  env declaration (a config string, not terminal bytes), resolves an engine name to the
  adapters' own spawn factory, and drives the registry's typed API — spawn, stop, reap —
  plus signal handling and bounded shutdown ordering. Every PTY and supervision fact
  (pump loop, restart semantics, reaping, the hangup path) stays in the session and
  registry modules the parenthetical names, which is the crate root doc's stated
  convention. No escape byte, no PTY syscall, no terminal fact is encoded here. The
  declaration holds.
- **`publish.rs`** (new) is channel plumbing and typed client calls as declared: the
  declaration parser, the forwarding loop, and the blank-grid byte arithmetic. That
  arithmetic mirrors the kernel hub's own constant and is wire-format serialization math,
  not terminal behavior; the blank-cell space-glyph convention it uses is `StyledCell`'s
  own in-repo doc. The restart-policy numbers it sets are configuration handed to the
  session module, which owns the semantics. The declaration holds.
- The other seven (`envelope`, `ingest`, `kernel_client`, `lib`, `logging`, `registry`,
  `wire`) are unchanged declarations whose files this diff touches lightly; each was
  re-read against its new content. `kernel_client`'s three new verbs are kernel-protocol
  requests, not terminal ones, still on the imported codec; `registry`'s `Attacher` is a
  clone of an existing command channel; `wire`'s new test prose is the previous record's
  finding 5 implemented — the fixture's SGR meanings are pinned, with citations, at the
  two named tests in `crates/gwk-pty/tests/style.rs`, which I confirmed exist and carry
  the ECMA-48 markers the prose points at.

**The pointer fix.** The previous record's finding 4 — a citation naming
`crates/gridwork/src/main.rs` for functions that live in the library — is discharged at
all five sites in this subject (`main.rs`, `logging.rs`, `ingest.rs`, `envelope.rs`
twice): `socket_path`, `envelope()`, `digest_of_json`, `rfc3339`/`civil_from_days` are
all in `crates/gridwork/src/lib.rs` (lines 229, 280, 521, 551/565), and
`crates/gridwork/src/main.rs` is a 12-line shim.

**Findings (three, non-blocking).** None blocks; each is a precision fix.

1. **`crates/gwk-pty/src/session.rs:237-238`** — the new marker's aside says "CAP-002
   registers the read side of the same event for the same reason." CAP-002's row
   registers the master-side read after the slave's last descriptor closes; this comment
   reasons about the slave side after the master closes — the mirror direction of a PTY
   endpoint disappearing, not the same event. "For the same reason" is exactly right (in
   both directions POSIX declines to specify the pairing, so the gap is either registered
   or disclaimed); "the same event" is one step looser than the row. Fix: "the mirror
   direction of the same pairing" or equivalent.
2. **`crates/gwk-pty/src/session.rs:236-237`** — "its pseudo-terminal mentions stop at
   control modes": the chapter also mentions pseudo-terminals at §11.1.1, opening the
   subsidiary side via `ptsname()`. The substantive claim — that the chapter contains no
   sentence about master-close presenting a disconnect — is confirmed true; only the
   inventory of where the mentions sit is narrow by one.
3. **`crates/gwk-pty-host/gwk-pty-host.service:74-77`** — the PrivateDevices rationale
   asserts an observable failure mode (a PTY allocation refused under the substituted
   private `/dev`, ENOENT before any session exists) in a config comment. The unit owes
   no marker by the gate's file-class rule, and the claim is about the deployment
   sandbox's namespace, not terminal protocol — recorded so the next reader knows it was
   looked at rather than skipped, not flagged for a citation the file class does not owe.

### framing

No source-derived framing. The publish family's vocabulary — attach, snapshot, deltas,
retire, publish, reseed — is the wire contract's own, and every structural decision in
the gated half states an in-repo origin I checked rather than accepted: the
one-connection-per-session shape from the client's own ask-one-question-at-a-time doc;
the standalone `Attacher` from the registry's stated lock-across-await hazard; the
sessions-stop-first shutdown ordering from the retire-versus-hangup distinction the wire
contract itself draws; the fatal-on-`validation` split from that code's judged-the-value
semantics as the error vocabulary defines it. The unit file's hardening relaxations each
argue from what an engine child needs, not from how any other tool's unit looks. The
declaration syntax (`name=engine:COLSxROWS`) is plain config-string shape. Nothing in
the gated diff reads as transcribed from another implementation.

### references

Class and hit count only, per this directory's rule. The corpus is the 1016-line gated
diff; the scanner was positive-controlled against a seeded term before the zero was
trusted (1 of 1 PASS).

- terminal-multiplexer / emulator / session-tool comparand class (11 terms): 0 hits.
- capture-cited-by-path class (1 pattern): 0 hits. Captures appear in the gated files as
  registered IDs only.

Registered permitted sources appearing, and which half of each I checked: for the two
registered names that are also implementations I read the **protocol document** at the
registered URL and no implementation source — the control-sequences reference page and
the colored-and-styled-underlines page. Both POSIX rows were read at their registered
`pubs.opengroup.org` locations. The engine names in the declaration syntax, its tests,
and the unit's example line are the subject engines this host exists to run — both
carry registered rows in `SPECS.md` — appearing as operator-config tokens matched
against the adapters' own `ENGINE` constants, not as comparands. The unit's `TERM` value
is a terminfo name in deployment configuration, with the comment explicitly declining to
claim it as a fact about any engine.

### leak

`./tools/leak-scan.sh` → `leak-scan: clean`, exit 0. `cleanroom-gate --markers` exit 0
over the eleven gated `.rs` files. The full gate was red only on this record's absence
before it was written, and names this subject.

Verdict: PASS. No escalation — the load-bearing new terminal fact (`kill()`'s reason to
exist) is correctly cited to `POSIX-TERM §11.1.10` and verified verbatim against the
chapter; the two reworded style markers now claim exactly what their pages say; the
publish/attach machinery itself derives from the repo's own wire contract and encodes no
terminal behavior of its own. The three findings are precision fixes to prose whose
underlying claims are true, and all five of the previous record's findings are
discharged in this subject.
