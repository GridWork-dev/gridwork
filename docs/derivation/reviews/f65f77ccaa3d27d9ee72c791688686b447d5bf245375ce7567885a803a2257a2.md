subject: f65f77ccaa3d27d9ee72c791688686b447d5bf245375ce7567885a803a2257a2
reviewer: fresh-context subagent dispatch, independent second-reader lane — no exposure to the implementing session; no SPEC, PLAN or kickoff document for this phase was read, and the stacked diff was read cold from `git diff --no-renames feat/7p-mux-scrollback...HEAD`
session: secondreader-7p-e4-r1-2026-08-09
date: 2026-08-09

# Second-reader record

## Scope

Two gated paths, the detach boundary made explicit. `crates/gwk-pty-host/src/registry.rs`
gains a doc-only addition to `attach`: two boundary facts (a detach is consumer-side only
and invisible to the child; attach carries no authorization — the kernel socket's
peer-credential boundary is what admits a consumer), with no code change and the file's
`Derivation: none` declaration unchanged. `crates/gwk-pty-host/tests/detach.rs` is new: an
integration test driving a real `/bin/sh` through the registry to demonstrate that a
detach delivers no SIGHUP, produces no end-of-file, and discards no queued input, carrying
three `Derivation: POSIX-TERM` markers (§11.1.3, §11.1.10, §11.1.11). No registry row is
touched.

## Checks

- [x] citations — every non-obvious terminal behavior carries a `Derivation:` marker
      naming a permitted source, and the marker sits on the behavior it actually
      describes. `cleanroom-gate` already proved every cited ID exists in
      `docs/derivation/SPECS.md` or `docs/derivation/CAPTURES.md`; what it cannot check,
      and you can, is whether the citation is TRUE — that the code does what that source
      says, and came from it
- [x] framing — no source-derived framing in comments, names, or structure
- [x] references — no naming of other projects as comparands, and no capture cited by
      path: captures are referenced by their registered ID and SHA-256, never otherwise.
      Record the finding as the CLASS searched and the HIT COUNT — never the query terms
      themselves. A name already registered in ../SPECS.md is a permitted source, not a
      query term: cite it, and say whether you checked the protocol or the implementation
- [x] leak — `./tools/leak-scan.sh` green

## Notes

Subject recomputed and matched: `git diff --no-renames --name-only
feat/7p-mux-scrollback...HEAD | ./tools/cleanroom-gate.sh --subject` →
`f65f77ccaa3d27d9ee72c791688686b447d5bf245375ce7567885a803a2257a2`, against a clean
working tree at HEAD `79bfdc5bc79e467b348b8e1381d46d4fd526b394`. `--markers` over the
same file list exits 0 (`registry.rs` declares no derivation; `detach.rs` cites
`POSIX-TERM` three times).

### citations

**`crates/gwk-pty-host/src/registry.rs` (`Derivation: none`, doc-only diff — the
declaration re-checked against the file as it now stands).** The added prose introduces
no code and asserts no external-behavior fact; every sentence in it is a first-party
claim, and each was verified against the code it describes rather than taken as prose:

- *"A detach is a consumer-side event only — the child cannot observe one, because the
  session task's side of the PTY outlives every attachment."* Verified in
  `src/session.rs`: the session task (`run`) owns the `gwk_pty::Session` for its whole
  life; the `Attach` command handler does exactly `deltas_tx.subscribe()` plus a
  catch-up computation and touches neither the PTY nor the child, and `Attached`'s own
  doc states "Dropping `live` IS detaching — there is no unregister step." Dropping an
  `Attached` drops a broadcast receiver and nothing else. TRUE.
- *"an attach carries a cursor and nothing else — no principal, no token, no
  per-consumer check."* Verified against the signatures: `attach(&self, id, cursor:
  Option<u64>)`, `SessionCommand::Attach { cursor, reply }`. No authorization state
  exists anywhere in the file. TRUE.
- *"the kernel's listener refuses any peer that is not its own effective uid before a
  single byte is parsed — `gwk-kernel`'s `wire::listen`."* Verified in
  `crates/gwk-kernel/src/wire/listen.rs`: `Listener::accept` calls `authorized` on the
  raw stream and drops a refused peer before the stream ever reaches the wire layer
  (the module doc states the credential "is checked before a single byte of the
  connection is read"); `admits` refuses any uid that is not `daemon_uid()`, which is
  `rustix::process::geteuid()` — the EFFECTIVE uid, exactly as the added prose says —
  and a peer whose credentials the OS cannot report is refused rather than assumed
  local. TRUE, in every particular.

The file remains a map of task handles and channel plumbing; the POSIX reasoning about
WHY nothing-closed means no consequences lives in `tests/detach.rs` under real markers,
not here. The `none` declaration REMAINS true.

**`crates/gwk-pty-host/tests/detach.rs` (new, three `POSIX-TERM` markers).** Each marker
was checked the hard way: the registered document for `POSIX-TERM` in
`docs/derivation/SPECS.md` (POSIX.1-2024 XBD §11 at
pubs.opengroup.org/onlinepubs/9799919799/basedefs/V1_chap11.html) was fetched and each
quoted fragment searched for in the named section.

- **§11.1.3** (module doc) — the section is titled "The Controlling Terminal" and
  contains every event the marker enumerates: allocation by the session leader
  ("allocated by the session leader in an implementation-defined manner"),
  relinquishment via `setsid()`, dissociation "when a controlling process terminates",
  and the quoted fragment "the close of the last file descriptor in the system" —
  verbatim, from the sentence "Upon the close of the last file descriptor in the system
  (whether or not it is in the current session) associated with the controlling
  terminal, it is unspecified whether all processes … cease to have any controlling
  terminal", which is precisely the "(whose effect it leaves unspecified even then)"
  parenthetical. The conclusion — a detach performs none of these, so the association is
  untouched — is explicitly labeled "this crate's reading of that enumeration."
  Non-blocking note: the section also names `fork()` inheritance, which the marker's
  enumeration omits; a detach is not a fork either, so the labeled inference is
  unaffected. The marker sits on the module doc, governing the whole-file behavior it
  describes. TRUE.
- **§11.1.11** (on the ACK:pending assertion) — the section is titled "Closing a
  Terminal Device File" and its first sentence reads "The last process to close a
  terminal device file shall cause any output to be sent to the device and shall cause
  any input to be discarded." The marker's elided quote "the last process to close a
  terminal device file ... shall cause any input to be discarded" is faithful: both
  fragments are verbatim and contiguous in the source (the sentence repeats "shall
  cause"), and the ellipsis marks the elided output clause. The inference — the queued
  line must survive a detach because the discard is bound to the LAST CLOSE and a detach
  closes no descriptor — is labeled "this crate's reading of that binding", and the
  marker sits directly on the assertion demonstrating it. TRUE.
- **§11.1.10** (on the GOT-HUP/SAW-EOF absence assertions) — the section is titled
  "Modem Disconnect" and contains all three quoted fragments verbatim: "if a modem
  disconnect is detected by the terminal interface for a controlling terminal" and "the
  SIGHUP signal shall be sent to the controlling process" (both from the section's first
  sentence), and "Any subsequent read from the terminal device shall return the value of
  zero, indicating end-of-file" (a full sentence, quoted whole). Non-blocking note: the
  marker's gloss "one trigger" elides the sentence's CLOCAL qualifier on the SIGHUP
  consequence; the test argues the contrapositive direction — no disconnect detected, so
  neither consequence — which holds regardless of the elided qualifier. The marker sits
  directly on the two absence assertions it licenses. TRUE.

The test body's in-repo claims also check out: "Detaching IS dropping the subscription —
`Attached`'s own contract" restates `session.rs`'s own doc verbatim in substance, and
"the attachment was never part of the input path" is structural fact — input travels the
`SessionCommand` channel, independent of the broadcast subscription. The child is
`/bin/sh`, honoring the crate's established rule that tests never exercise a real engine
binary.

### framing

First-principles throughout. The test's structure is derived from the specification's
own consequence list — each consequence a detach must not have becomes a line the child
would print (`GOT-HUP`, `SAW-EOF`, `ACK:<line>`), and the clean `exit 0` is the closing
argument — which is spec-backward test design, not transcription. Names
(`a_detach_is_invisible_to_the_child`, `await_rows`, `rows_text`, `SCRIPT`) are plain
descriptions; the registry prose is boundary-defensive first-party doc writing in the
merge-base file's established long-form voice. Nothing reads as carried over from
another implementation.

### references

Class and hit count only, per this directory's rule. Searched both gated files in full
(the new test plus the whole of `registry.rs`). Both term lists were positive-controlled
against a seeded scratch file outside the repo before any count was trusted — 2 of 2
control hits — and the scratch files and term lists were deleted after the run.

- terminal-multiplexer / terminal-tooling comparand class (10 terms, case-insensitive
  substring): 10 raw matches, 0 comparand references. Nine were the common English
  display-surface noun in ordinary prose (six in `registry.rs`, three in `detach.rs`);
  one was the crate's own openly-declared pinned VT engine dependency name inside a
  pre-existing merge-base comment in `registry.rs`, outside this diff's hunks — a
  permitted declared dependency, not a comparand.
- registered `../SPECS.md` multiplexer/terminal names (4 terms): 0 hits. Neither file
  names a registered mux or terminal at all, so no protocol-vs-implementation
  disclosure is owed.
- capture-cited-by-path class (2 patterns): 0 hits. No capture is cited in this diff at
  all, by ID or otherwise.

### leak

`./tools/leak-scan.sh` → `leak-scan: clean`, exit 0. `./tools/check-claims.sh` →
`check-claims: clean`, exit 0.

Verdict: PASS. Zero blockers. The one `none` declaration this diff touches stays true —
the added prose asserts only first-party facts, and each was verified against the code
it names, including the kernel listener's same-EUID refusal in
`crates/gwk-kernel/src/wire/listen.rs`. All three new `POSIX-TERM` citations quote real
sentences from the sections they name, sit on the behaviors they license, and label
every step past the quoted text as the crate's own reading. Two non-blocking notes are
recorded above (the §11.1.3 enumeration omits `fork()` inheritance; the §11.1.10 gloss
elides the CLOCAL qualifier); neither affects the direction either marker is used in.
