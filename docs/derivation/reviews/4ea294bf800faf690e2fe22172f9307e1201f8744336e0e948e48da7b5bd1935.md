subject: 4ea294bf800faf690e2fe22172f9307e1201f8744336e0e948e48da7b5bd1935
reviewer: fresh-context subagent dispatch, independent second-reader lane — no exposure to the implementing session; no SPEC, PLAN or kickoff document for this phase was read, and the stacked diff was read cold from `git diff --no-renames feat/7p-mux-arrangement...HEAD`
session: secondreader-7p-e8-r1-2026-08-09
date: 2026-08-09

# Second-reader record

## Scope

Two gated paths in `crates/gwk-adapter-opencode/src/`, doc-and-marker-only — no code
changes anywhere in the diff. `lib.rs` gains a new `OPENCODE-PLUGINS` marker on
`spawn_tui` and extends the `OPENCODE-ACP` marker on `acp_agent_config`; `event.rs`
extends the `OPENCODE-SERVER §Events` marker on the `StreamConnected` variant. Each of
the three markers has the same unusual shape: it quotes what its registered document
DOES say, then records what the document does NOT say, and labels the consequence drawn
from that silence as this crate's own reading rather than a sentence the document
contains. A silence claim cannot be checked against the diff, so all three registered
documents were fetched live and searched in both directions.

## Checks

- [x] citations — every non-obvious terminal behavior carries a `Derivation:` marker
      naming a permitted source, and the marker sits on the behavior it actually
      describes. `cleanroom-gate` already proved every cited ID exists in
      `docs/derivation/SPECS.md` or `docs/derivation/CAPTURES.md`; what it cannot check,
      and you can, is whether the citation is TRUE — that the code does what that source
      says, and came from it
- [x] framing — no source-derived framing in comments, names, or structure
- [x] references — no naming of other projects as comparands, and no capture cited by
      path: captures are referenced by their registered ID and SHA-256, never otherwise.
      Record the finding as the CLASS searched and the HIT COUNT — never the query terms
      themselves. A name already registered in ../SPECS.md is a permitted source, not a
      query term: cite it, and say whether you checked the protocol or the implementation
- [x] leak — `./tools/leak-scan.sh` green

## Notes

Subject recomputed and matched: `git diff --no-renames --name-only
feat/7p-mux-arrangement...HEAD | ./tools/cleanroom-gate.sh --subject` →
`4ea294bf800faf690e2fe22172f9307e1201f8744336e0e948e48da7b5bd1935`, against a clean
working tree at HEAD `0c11c053fe53a197aaa3e214b36bb404143fcdbd`. `--markers` over the
same file list resolves every citation: `event.rs` → `OPENCODE-SERVER` (×9) +
`OPENCODE-PLUGINS`; `lib.rs` → `OPENCODE-PLUGINS` + `OPENCODE-ACP`.

### citations

Because every new claim in this diff is a claim about a document — including three
claims of documentary *silence* — each registered page was fetched live on the review
date and searched whole, not summarized: `OPENCODE-ACP` (opencode.ai/docs/acp),
`OPENCODE-SERVER` (opencode.ai/docs/server), `OPENCODE-PLUGINS`
(opencode.ai/docs/plugins). Both directions were checked for each marker: every quoted
fragment must appear on the page, and every claimed absence must be genuinely absent.
All three pages reported "Last updated: Aug 9, 2026" — the review read the documents as
they stand today, which is the only sense in which a silence claim is checkable.

**`lib.rs` `spawn_tui` — new `OPENCODE-PLUGINS` marker.** Every positive claim is on the
page: `.opencode/plugins/` is listed as "Project-level plugins" and
`~/.config/opencode/plugins/` as "Global plugins", npm packages come from the config
file, and the plugin context hands `directory` ("The current working directory.") and
`worktree` ("The git worktree path.") as two separately listed values — the marker's
quotes are verbatim. The silence holds: the page's loading rules are directory
membership, config entries, and a four-step load order, and no sentence anywhere on the
page connects the working directory to which plugins load — "project" is never defined
in terms of cwd. The consequence is correctly labeled as the crate's own: "an inference
the document does not license, and this crate does not make it." The marker sits on
`spawn_tui`, the render-half spawn, which is exactly where a pane-cwd-selects-plugins
assumption would otherwise attach. Registry scope note: the `OPENCODE-PLUGINS` row's
descriptive text names the event-bus lists, but SPECS.md's own scope rule is
document-level ("a row is scoped to the document it names"), and every fact this marker
claims is on the named document — in scope.

**`lib.rs` `acp_agent_config` — extended `OPENCODE-ACP` marker.** The quotes are
verbatim: "works the same via ACP as it does in the terminal. All features are
supported" is the page's Support-section sentence, and its exception — "Some built-in
slash commands like `/undo` and `/redo` are currently unsupported" — sits directly
under it in a Note, named and time-qualified exactly as the marker says. The parity
claim is indeed a claim about features: the sentence introduces a bulleted feature list.
All four claimed silences hold on the whole page: no statement about session persistence
(the word "session" does not appear at all), none about detach or reattach, none about
multiple simultaneous clients, none about stderr — the page's only transport sentence is
"JSON-RPC via stdio". (The "multiple clients" sentence lives on the SERVER page, not
this one, which is precisely the boundary the marker draws.) The consequence is labeled
honestly: "this crate's reading of the document's scope, not a sentence it contains."
The doc comment's other half — that persistence/detach/multi-client are properties of
the PTY session path — is a first-party claim about this workspace's own architecture,
not a derivation, and needs no source.

**`event.rs` `StreamConnected` — extended `OPENCODE-SERVER §Events` marker.** The
retained quote is verbatim: the `/event` row reads "Server-sent events stream. First
event is `server.connected`, then bus events". The silence holds: `GET /global/event`
is listed with description "Get global events (SSE stream)" and response "Event
stream", with no first-event sentence — and `server.connected` appears exactly once on
the whole page, on the `/event` row. So "stated for that stream ONLY" is a verified fact
about the document, and the downstream prohibition is a consequence correctly drawn on
this side of the citation. The marker and its new doc paragraph sit on the
`StreamConnected` variant, the one artifact whose consumers key readiness on that first
event.

Non-blocking notes, two. First, the `event.rs` marker says "the same endpoint table
lists `GET /global/event`": on the page, `/global/event` sits in the §Global sub-table
and `/event` in the §Events sub-table of the same APIs listing — same page, same
endpoint listing, different sub-table. The material claim (listed, with no first-event
sentence anywhere) is true and the licensed consequence unchanged; the phrase is a
layout gloss, not a wrong-section citation. Second, the `spawn_tui` doc comment's middle
clause ("a pane's working directory does not select the engine's plugin set") read in
isolation could sound like an engine-behavior fact derived from silence; read with its
frame — "nothing is asserted about what the engine loads" before it, "no artifact in
this workspace may claim it does" after it, and the marker's "this crate does not make
it" below — it is a workspace scope declaration, which the document's silence does
license.

Pre-existing markers: the diff is three hunks (one in `event.rs`, one spanning both
functions in `lib.rs`); every other marker in both files — the module docs, `BusEnvelope`,
`SessionStatus`, `ToolRef`, `PermissionAsk`, `SessionErrorInfo`, both `normalize`
markers, `PermissionDecision`, `reply_action` — is outside the hunks and byte-identical
at HEAD.

### framing

The added prose is analytic and in the repository's own argued voice: it quotes the
registered documents in quotation marks with attribution, then reasons about their scope
in first person ("this crate's reading", "this crate does not make it"). No new names —
`spawn_tui`, `acp_agent_config`, `StreamConnected` all predate this diff — and no new
structure. Nothing reads as transcribed from another implementation; the entire diff is
about declining to transcribe what no document states.

### references

Class and hit count only, per this directory's rule. Searched the full stacked diff.
Every term list was positive-controlled against a seeded scratch file outside the
repository before any count was trusted — each list hit its control — and the lists and
seeds were deleted after the run.

- terminal-tooling comparand class (8 terms, case-insensitive): 0 hits.
- quarantined-artifact filename class (4 terms): 0 hits.
- capture-cited-by-path class (2 patterns): 0 hits. No capture is cited in this diff at
  all, by ID or otherwise.
- opencode is named throughout, as a registered permitted source (`OPENCODE-ACP`,
  `OPENCODE-SERVER`, `OPENCODE-PLUGINS`) — cited freely per the rule. What this review
  checked is the registered documentation pages themselves, fetched live; no opencode
  implementation source was read, and the diff cites none.

### leak

`./tools/leak-scan.sh` → `leak-scan: clean`, exit 0. `./tools/check-claims.sh` →
`check-claims: clean`, exit 0.

Verdict: PASS. Zero blockers. All three markers quote their documents accurately, every
claimed silence was verified against the live registered page rather than assumed, each
consequence drawn from a silence is explicitly labeled as this crate's own reading, and
each marker sits on the exact artifact its claim protects. Two non-blocking notes are
recorded above (the "same endpoint table" layout gloss; the `spawn_tui` middle clause
that reads normatively only with its surrounding frame).
