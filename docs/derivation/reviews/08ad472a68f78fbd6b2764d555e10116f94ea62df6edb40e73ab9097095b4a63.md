subject: 08ad472a68f78fbd6b2764d555e10116f94ea62df6edb40e73ab9097095b4a63
reviewer: general-purpose fresh-context dispatch — no exposure to the implementing session (routed to the catch-all lane per CLEANROOM.md rule 4; a specialized house agent would carry exactly the prior-exposure surface the control exists to exclude)
date: 2026-08-02

# Second-reader record

## Scope

`crates/gwk-adapter-claude/src/lib.rs` — `control_command()` gains `--verbose`, carrying
a new `CAP-003` marker (the capture registered and committed in the same change) beside
the existing `CLAUDE-STREAM-JSON` marker; the change also hardens the non-gated parity
runner against a dead stream-json channel reading as an empty fan-out.

## Checks

- [x] citations — every non-obvious terminal behavior carries a `Derivation:` marker
      naming a permitted source, and the marker sits on the behavior it actually
      describes. `cleanroom-gate` already proved every cited ID exists in
      `docs/derivation/SPECS.md` or `docs/derivation/CAPTURES.md`; what it cannot check,
      and you can, is whether the citation is TRUE — that the code does what that source
      says, and came from it
- [x] framing — no source-derived framing in comments, names, or structure
- [x] references — no naming of other projects as comparands, and no capture cited by
      path: captures are referenced by their registered ID and SHA-256, never otherwise
- [x] leak — `./tools/leak-scan.sh` green

## Notes

Read cold on 2026-08-02: CLEANROOM.md, this directory's README, the full
`main...HEAD` diff, both registries, and the committed capture. Subject recomputed via
the README's command with `CLEANROOM_BASE=$(git merge-base main HEAD)` — matched this
record's name; the gate correctly folded the edited `CAP-003` row in as touching the
citing gated file.

### citations

Both markers on `control_command()` verified true against their sources:

- `CLAUDE-STREAM-JSON` — the row's scope (print-mode `--input-format`/`--output-format`,
  stream-json on both, stdin/stdout) is exactly what the invocation builds, and the live
  cli-reference page confirms both flags with stream-json among their options.
- `CAP-003` — the capture carries its probe, so it was re-run rather than believed.
  Installed CLI is exactly the pinned 2.1.220. The probe
  (`env -u ANTHROPIC_API_KEY timeout 10 claude --print --input-format stream-json
  --output-format stream-json </dev/null`) returned stderr
  `Error: When using --print, --output-format=stream-json requires --verbose` and exit
  status 1 — byte-for-byte the committed `observed:` block — with zero bytes on stdout,
  which supports the row's "refuses at flag validation before either format flag takes
  effect" characterization (auth-independent by construction: the API key is unset).
  `sha256sum` of the committed capture file is
  `f387911b368f608bbdbc332981ef7a70db99fffebb42affaa00174c33288b2c7`, matching the row.
  The marker's quoted error string and exit status match the capture exactly.

The row's registry prose makes a load-bearing absence claim — that neither
`CLAUDE-STREAM-JSON`'s nor `CLAUDE-HEADLESS`'s page states the dependency, which is why
the requirement takes the own-observation clause. Verified against both live pages on
2026-08-02: the cli-reference page documents `--verbose` and `--output-format`
independently with no stated dependency; the headless page pairs the flags only inside
instructional example text and never states a requirement. The clause's reasoning holds
and mirrors `CAP-002`'s false-citation-dressed-as-real test correctly. The decision NOT
to commit the positive (`--verbose`-added) probe because its output carried live local
session state is the right call under the registry's publishability rule.

Two nits, recorded, non-blocking:

1. The prose quotes the headless page's example as "Use `--output-format stream-json`
   with `--verbose` to see..." — the page as of 2026-08-02 reads "...with `--verbose`
   and `--include-partial-messages` to receive tokens as they're generated." Likely doc
   churn since capture; the substantive claim (pairing only in an example, no stated
   requirement) is true of the current page either way. Worth syncing on a future touch.
2. The committed capture records stderr and exit status but not stdout; the row's
   "before either format flag takes effect" leans slightly on stdout silence, which my
   re-run confirmed (0 bytes) but the capture file does not itself record.

### framing

Comments and names in the gated file and the parity runner read as derived from the
registered observation and this repo's own design language (`has_terminal_result`,
"dead channel", vacuous-pass reasoning). Nothing reads as transcribed from another
implementation. Pass.

### references

No other project is named as a comparand anywhere in the diff — Claude Code is the
engine under adaptation, and the two doc pages discussed are registered `SPECS.md` rows
referenced by ID. The gated file cites `CAP-003` by ID only; the capture's path exists
only as the committed file under `docs/derivation/captures/`, the directory the
registry itself sanctions naming. Pass.

### leak

`./tools/leak-scan.sh` from the worktree root → "leak-scan: clean", exit 0. Pass.

Verdict: PASS.
