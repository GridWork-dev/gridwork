subject: 636b4d0155ef34f0a32a389706a419cc58aa93d47d43ee384d20c59a3908e480
reviewer: fresh-context subagent dispatch, independent second-reader lane — no exposure to the implementing session; no SPEC, PLAN or kickoff document for this phase was read, and the diff was read cold from `git diff --no-renames main...HEAD`
session: secondreader-host-runtime-r1-2026-08-06
date: 2026-08-06

# Second-reader record

## Scope

Fourteen gated paths, twelve of them marker-owing. Four under `crates/gwk-pty/`:
`src/render.rs` gains `Row::cols` (the zero-based starting column of each pushed
cluster, index-aligned with `styles`) plus a wide-cluster test; `src/session.rs` gains
`pump_chunk` (returning the bytes, with `pump` becoming a `.len()` wrapper) and `kill`;
`src/style.rs` and `tests/style.rs` change only marker prose. Ten under
`crates/gwk-pty-host/`, where the crate stops being a kernel-facing skeleton and grows
its session runtime: `src/session.rs` (new — one supervision task per session over
`gwk_pty::Session`, restart policy, retained delta batches, attach/catch-up),
`src/wire.rs` (new — engine `Frame` to the wire contract's `PtyFrame`/`PtyDelta`),
`src/registry.rs` (new — sessions by id, thread-per-session, the six verbs),
`src/engines.rs` (new — engine name to adapter spawn factory), plus `src/origination.rs`
(gains `originate_record` and the stale-transition retry), `src/kernel_client.rs` (gains
`dispatch_node_version`), `src/lib.rs`, `src/main.rs`, `Cargo.toml` (takes the engine and
three adapter dependencies, ending the crate's no-Zig build property) and the systemd
unit. Neither registry file is touched by this diff, so the gate needed no
`CLEANROOM_BASE` to compute the subject.

The three marker rewordings in `crates/gwk-pty/` are this branch discharging the previous
record's three findings; each is checked below against the source rather than against the
finding that asked for it.

## Checks

- [x] citations — every non-obvious terminal behavior carries a `Derivation:` marker
      naming a permitted source, and the marker sits on the behavior it actually
      describes. `cleanroom-gate` already proved every cited ID exists in
      `docs/derivation/SPECS.md` or `docs/derivation/CAPTURES.md`; what it cannot check,
      and you can, is whether the citation is TRUE — that the code does what that source
      says, and came from it
- [x] framing — no source-derived framing in comments, names, or structure
- [x] references — no naming of other projects as comparands, and no capture cited by
      path: captures are referenced by their registered ID and SHA-256, never otherwise.
      Record the finding as the CLASS searched and the HIT COUNT — never the query terms
      themselves. A name already registered in ../SPECS.md is a permitted source, not a
      query term: cite it, and say whether you checked the protocol or the implementation
- [x] leak — `./tools/leak-scan.sh` green

## Notes

Subject recomputed and matched:
`git diff --no-renames --name-only main...HEAD | ./tools/cleanroom-gate.sh --subject`
→ `636b4d0155ef34f0a32a389706a419cc58aa93d47d43ee384d20c59a3908e480`, against a clean
working tree at HEAD `1ced5f2` (merge base `767e8fc`). `--markers` reports the twelve
`.rs` files — eight host files declaring no derivation, four `gwk-pty` files citing — and
exits 0. No file pairs a `none` with a real citation. `cargo test -p gwk-pty --lib` 27
passed, `cargo test -p gwk-pty-host --lib` 44 passed; I also ran
`cargo test -p gwk-pty --test style` (25 passed, both segmentation tests included) and the
render suite (6 passed, the new `a_wide_cluster_advances_the_column_by_two` among them),
under the pinned toolchain environment.

One methodology note that changed a number: every command feeding a count, a corpus or a
redirect in this review was run through the shell-filter bypass. The first attempt to
build the added-lines corpus returned 0 lines, then 47, for a diff with 2304 insertions —
silent truncation with exit 0. The corpora the reference counts below are computed over
are the re-run ones (2304 added lines, 3997 lines of gated files at HEAD), verified
against the diffstat.

### citations — every cited source read at its registered location this round

I did not inherit any verification from the previous record. Sources read: `ECMA-48` as
the 5th-edition PDF the registry's page links (354 pages, `pdftotext -layout`);
`XTERM-CTLSEQS`, `KITTY-UNDERLINES`, `UAX-11`, `UAX-29`, `POSIX-WINSIZE` and
`TERM-UNICODE-CORE` each at the location its registry row names; `CAP-002` and `CAP-004`
as the committed captures, hashes recomputed. `CAP-001` and `CAP-003` are cited by
nothing in this subject.

**The three rewordings.**

- `tests/style.rs:309-313` (UAX-29, GB9/GB11/GB999) is now correct and correctly
  decomposed. GB9 is `× (Extend | ZWJ)` and is unconditional; GB11 is
  `\p{Extended_Pictographic} Extend* ZWJ × \p{Extended_Pictographic}`; GB999 is
  `Any ÷ Any`. The marker's three clauses map one-to-one onto those, and WOMAN (U+1F469)
  and ROCKET (U+1F680) are both extended pictographic, so the conclusion for this input
  follows from GB11 rather than from an unconditional reading of it. That is exactly what
  the previous finding asked for.
- `tests/style.rs:271-274` (XTERM-CTLSEQS, the `58:2::70:80:90` form) supplies the link
  the previous record found missing, and supplies it from the right document: the page
  carries `Ps = 3 8 : 2 : Pi : Pr : Pg : Pb ⇒ Set foreground color using RGB values`
  verbatim, so `2`-means-direct-RGB is that page's and not inferred. See finding 2 for
  the one clause that outruns it.
- `src/style.rs:24-32` (ECMA-48 §8.3.117, the palette marker) is now true where it was
  previously a characterization. §8.3.117 lists 30-37 as `black display` … `white display`
  and 40-47 as their background counterparts — eight named colours each, as bare parameter
  values — and it nowhere calls them index selectors. The marker now says the slot reading
  is "this crate's reading of that shape, not a sentence either document contains", which
  is the finding's fix and is itself accurate: I checked both documents for it.

**The new UAX-11 marker** at `src/render.rs:351-353` is the correct shape. U+5BEC falls in
`4E00..9FFF ; W` in the UCD's `EastAsianWidth.txt`, so East Asian Wide is a fact, and the
marker attributes the two-columns conclusion to "the two-cells-per-wide-character model
the loop above reads into the annex's Em ratio" — pointing at `src/render.rs:142-150`,
which still carries the disclaimer that "two columns" is the crate's reading and not a
sentence UAX-11 contains. UAX-11 does say Wide takes "one Em of space" against "1/2 Em"
for the halfwidth and narrow classes, and does carry the note that the property "is not
intended for use by modern terminal emulators without appropriate tailoring on a
case-by-case basis". A citation pointing at another citation for the inferential step,
instead of restating the conclusion as a spec fact, is the right way to add a second site.

**Everything else cited in the four `gwk-pty` files**, since the whole file is in-subject
even where the diff did not touch the line: every parenthesized SGR gloss in `src/style.rs`
and `tests/style.rs` is verbatim against §8.3.117 (1, 2, 3, 4, 5, 7, 8, 9, 21, 22, 23, 24,
27, 29, 31, 39, 44, 49, 53, 0 — checked one at a time; the SGR 5 and SGR 9 quotes truncate
before a parenthetical that drops no meaning). `§7.2.8` really does define exactly two GRCM
settings, REPLACING and CUMULATIVE, so the conditional the attribute-pile test states is
the spec's own. `§5.4` gives the control-sequence format as `CSI P ... P I ... I F` with
Parameter, Intermediate and Final bytes, which is precisely the fact making a byte-boundary
split hazardous — and the marker rode the `grid.write` line correctly from `pump` into
`pump_chunk`. `§8.3.9` (CHA), `§8.3.20` (CUF) and `§8.3.21` (CUP) resolve to those
functions with the stated 1-based parameters and `Pn = 1` defaults. Both `POSIX-WINSIZE`
markers are exact, including the harder half: the page says "No signal shall be delivered
if the terminal size was changed to the same value it had before the `tcsetwinsize()`
call", which is the changed-not-merely-successful distinction the comment insists on, and
"Otherwise, -1 shall be returned, the terminal window size shall not be changed" for the
rollback marker. The errno list in that block's `ponytail:` note (EBADF, ENOTTY, EIO,
EINVAL) matches the page's ERRORS section term for term. `TERM-UNICODE-CORE` checks out at
its specification source: `\SetWatermarkText{Draft}`, `\date{2021-09-06 (draft, revision
1)}`, `\newcommand\VtModeNum{2027}`, "With this mode enabled, the terminal **MUST** support
grapheme clusters in conformance to algorithm as described in" UAX-29, and "If the VT mode
2027 is not set, then the behavior is as undefined as if this specification was not
implemented at all" — and the hosting API's license endpoint still 404s, so the row's
no-license disclosure holds. `KITTY-UNDERLINES` confirms `4:3` curly, `4:4` dotted, `4:5`
dashed, and that 58 "works exactly like the codes `38, 48`".

**`CAP-002` and `CAP-004`** both re-verify. The three committed captures hash to exactly
the three registered values. `CAP-002`'s probe is eleven lines of standard-library Python
and its observed `errno 5 (EIO)` is the fact `src/session.rs:97` cites it for. `CAP-004`'s
probe drives this crate's public API with the same helper `tests/style.rs` uses; its
arithmetic is internally consistent and I recomputed it (10-column grid, each emoji two
columns, a spacer contributes no entry: 2 clusters + 6 blanks = 8, 1 + 8 = 9 — the two
recorded lengths). Worth recording because this change edits the struct that probe reads:
it reads `row.styles` only, so adding `Row::cols` leaves the capture re-runnable against
HEAD and its hash untouched.

**The eight `Derivation: none` declarations, each read as a claim.** Mechanically: across
all of `crates/gwk-pty-host/src/`, zero escape-byte, CSI, OSC or DCS forms appear in any
non-test code; the two non-test occurrences of "SGR" anywhere in the crate are the words in
`wire.rs`'s doc comment explaining the palette ruling; and there is no `libc`, `nix`,
`rustix`, `ioctl`, `termios` or `TIOCSWINSZ` anywhere in the crate at all. Every
`Command::new` in the crate is in test code, spawning `/bin/cat`, `/bin/sh`, `/bin/true`
or a deliberately-missing path, which the test module states as a rule.

- `lib.rs`, `main.rs`, `kernel_client.rs`, `origination.rs`, `engines.rs` — true as
  written and unremarkable. `main.rs` genuinely starts no session (its own doc says so and
  the file confirms it: tracing sink, one health check, `notify_ready`, two-signal wait).
  `kernel_client.rs`'s new `dispatch_node_version` adds a kernel-protocol request, not a
  terminal one, and still imports the codec rather than reimplementing it.
  `engines.rs` re-spells no engine name — it matches on the adapters' own `ENGINE`
  constants, and its test does too.
- `session.rs` (host) is the one the prompt rightly singles out, and its declaration is the
  honest form: it says "process supervision" outright and claims only that it derives
  nothing. I checked every PTY fact it consumes — `pump_chunk`, `wait`, `kill`, `send`,
  `resize`, `grid`, `Renderer::frame`, `Recording::record` — and each is a typed method on
  the engine crate's public API. The one engine-behavior fact it leans on beyond a
  signature ("the reflow marked the grid fully dirty; take the frame NOW") is a documented
  property of the in-repo renderer, whose own markers cover it. Its supervision policies —
  respawn on failure but never on a clean exit, a fixed delay with the unit's `RestartSec`
  named as the outer layer, a same-size `Resized` as the "your coordinates are void"
  signal after a respawn — are each self-justified in place, and none of them is a terminal
  fact.
- `registry.rs` — true, and the word "directly" is load-bearing, which is worth writing
  down so the next reader does not re-derive it. The first `spawn(...)` call site IS in
  this file, but it invokes an opaque caller-supplied factory: the file names no program,
  builds no argv, holds no pid and signals nothing, and the things it starts itself are
  threads, not processes. Its thread-per-session structure rests on a stated property of
  the dependency — that the engine's grid and render state hold raw pointers and are not
  `Send` — which I verified by compile probe rather than accepting: a temporary
  `assert_send::<gwk_pty::Session>()` fails with `NonNull<TerminalImpl> cannot be sent
  between threads safely`. Probe written, run, deleted; tree clean before and after.
- `wire.rs` — the `none` is true as written (this file parses nothing; it maps typed engine
  values field-for-field into wire types), and I checked the "twelve facts, a rename not a
  projection" claim rather than trusting it: the engine struct has twelve public fields,
  the wire struct has twelve, and `style()` assigns twelve. See finding 5 for the one test
  fixture that deserves a sentence of prose.

**Findings (five, non-blocking).** None blocks. Each is a wording or pointer fix.

1. **`crates/gwk-pty/src/session.rs:224-225`** — the new `kill` doc asserts an uncited
   terminal-interface fact: "Dropping a `Session` closes the PTY master, which only
   *signals* the child (SIGHUP) — a child that ignores it lingers." No `Derivation:` marker
   sits on it. I considered the blocking reading and rejected it: `kill()` is
   `child.kill()`, which bypasses the hangup path entirely, so nothing in the function
   derives from the claim and a wrong version of it would be a wrong comment rather than
   wrong behavior — and no impermissible source is named. But it is the same family as
   `CAP-002`, which this repository registered rather than assumed for the *read* side of
   the very same event, so asserting the *signal* side uncited sits oddly against its own
   precedent. This file names its sources, so a marker may be added without tripping the
   no-`none`-beside-a-citation rule; `POSIX-TERM` (XBD §11) is where the disconnect-sends-
   SIGHUP rule lives, and `SPECS.md`'s own header warns that the chapter row and the
   function row are different rows, which is the trap to avoid if it is added. Note also
   that the sentence is understated in the code's favour: the hangup reaches only the
   foreground process group of the session for which that terminal is controlling, so a
   detached child gets nothing at all — which makes `kill`'s reason for existing stronger,
   not weaker.
2. **`crates/gwk-pty/tests/style.rs:271-274`** — "the colour-space slot between it and the
   components may be left empty" is one inferential step past what the page says, which is
   "The color space identifier _Pi_ is ignored." Ignored is not the same permission as
   may-be-empty, and the only form on that page which actually omits `Pi` is the
   semicolon-delimited konsole variation (`38 ; 2 ; Pr ; Pg ; Pb`), not a colon form with a
   hole in it. Nothing is wrong — the `2`-means-direct-RGB half is verbatim, xterm does
   accept colons "(standard)", and the empty slot is pinned by assertion against the engine
   rather than claimed from the spec. Fix: say that xterm ignores the colour-space
   identifier in that slot, which is why the shape below parses, instead of granting a
   permission the page does not write.
3. **`crates/gwk-pty/tests/style.rs:269-270`** — the paired KITTY-UNDERLINES marker
   attributes "SGR 58 **with colon-separated sub-parameters**" to that page. The page shows
   only `<ESC>[58...m` and the "works exactly like the codes `38, 48`" sentence; it spells
   no colon form for 58. The colon qualifier is licensed by the XTERM marker directly
   beneath it, so the pair as a whole is now sound and the previous record's second finding
   is genuinely discharged — but the qualifier belongs on the XTERM half. Fix: drop
   "with colon-separated sub-parameters" from the KITTY line and let it carry only what
   that page states, which is that 58 sets the underline's own color.
4. **`crates/gwk-pty-host/src/main.rs:20`** — carry-over, still open, and inside this
   subject. It cites `crates/gridwork/src/main.rs` for the socket-path lookup; that
   function is at `crates/gridwork/src/lib.rs:229` and `crates/gridwork/src/main.rs` is a
   12-line shim (I re-confirmed both). This is the previous record's first finding,
   unfixed. Four of its five sites — in `logging.rs`, `ingest.rs` and `envelope.rs` twice —
   are outside this change's touched set and so outside my subject and unreviewable here,
   but this one is inside it and would pass a second review unremarked if I did not say so.
5. **`crates/gwk-pty-host/src/wire.rs:363`** — the only terminal-protocol byte sequence in
   any host file, in a file declaring "no terminal-protocol byte is parsed". The `none`
   holds: `wire.rs` parses nothing, the sequence is handed to the engine's `Grid::write`,
   and the assertion is about the conversion preserving whatever the engine reported. But
   the test does assert SGR 1 → bold and SGR 31 → palette index 1, and a reader can take
   that for an uncited SGR claim. Because a `none` may not sit beside a real citation, the
   fix is prose rather than a marker: say in the test that the sequence is a fixture whose
   meaning is pinned in `crates/gwk-pty/tests/style.rs:35,110`, where both facts already
   carry their ECMA-48 §8.3.117 citations.

Two precision notes carried forward unchanged from the previous record, re-confirmed
against this content so the next reader need not re-derive them: `CAPTURES.md`'s `CAP-004`
note undercounts the specification's undefined-state statements (I count four — backwards
compatibility, the mode-switching bullet, the semantics preamble, terminal reset), and
`src/render.rs:29-30`'s "extended grapheme clusters" is a reading of a source that says
"grapheme clusters in conformance to" UAX-29 without the word, which UAX-29 recommends.
Both remain understated rather than overstated.

### framing

No source-derived framing. The supervision design is explicable end to end from the engine
crate's own API plus in-repo contracts, and I traced each structural decision to its stated
origin rather than accepting the assertion: one task per session with a command channel
standing in for the borrow, from `gwk_pty::Session`'s own doc that "the read loop is the
caller's" (verbatim at `crates/gwk-pty/src/session.rs:41`); thread-per-session with a
current-thread runtime, from the engine types being `!Send` (compile-probed above); the
attach/catch-up split, mirrored on the in-repo `gwk_pty::attach::catch_up` and
`Recording::since`'s eviction refusal, both of which exist where the comment says
(`attach.rs:83`, `attach.rs:107`, `record.rs:162`); the resize-invalidates-coordinates rule
and the empty-glyph trailing half, from `gwk-domain`'s frame module, whose sentences I
checked against the file rather than the quotation ("terminal grid into this shape is a
later task", "An empty string is a legal glyph: a blank cell, or the trailing half of a
double-width character", "The variants match what `gwk_pty::Underline`"). The crate-root
doc's boundary claim also checks out: `serve.rs:475` contains the quoted sentence verbatim,
and `FRAME_KIND_RESERVED_STREAM: u8 = 0x02` exists in `protocol.rs:196`. The one functional
vocabulary that could read as borrowed — spawn, pump loop, restart semantics, session
registry, detach/reattach routing — is quoted from CLEANROOM.md rule 2's own category
definition and attributed as such in the crate root doc. A separate sweep for
conventional-wisdom and other-implementation phrasing ("the usual", "prior art", "other
implementations", and eight more) returned 0 hits across both corpora. Two deliberate
simplifications carry `ponytail:` comments naming their ceiling and upgrade path (the fixed
restart delay; interior multi-codepoint cluster recovery in `split_clusters`) — disclosure,
not transcription.

### references

Class and hit count only, per this directory's rule. Each class ran over two corpora — the
2304 added lines of the full diff, and the fourteen gated files at HEAD — and the scanner
was positive-controlled against a seeded string from every class before any zero was
trusted (5 of 5 controls PASS).

- terminal-multiplexer / session-manager comparand class (13 terms): 53 raw hits in the
  added lines, 60 in the gated files — every single one the English word "screen" or the
  `WireScreen` type this change introduces. The twelve project-name terms score zero in
  both corpora. 0 comparand uses.
- terminal-emulator / VT-implementation comparand class (26 terms): 17 raw hits in the
  added lines, 36 in the gated files, resolving to exactly three names, all accounted for
  below. 0 comparand uses.
- PTY-supervision-tool comparand class (16 terms): 85 raw hits in the added lines, 137 in
  the gated files, every one the Rust `.expect(...)`/`.expect_err(...)` method or the words
  "expected"/"expects" in prose — I enumerated the residue and the single non-method
  occurrence is the English verb in a sentence about the systemd unit. 0 project-name uses.
- derivation-by-comparison phrasing class (14 terms): 0 hits in both corpora.
- capture-cited-by-path class (2 patterns): 0 hits in both corpora. No `Derivation:` marker
  and no prose cites a capture by path anywhere in this change, and neither registry file
  is touched.

Registered permitted sources, and which half of each I checked. For the two registered
names that are also implementations I read the **protocol document** at the registered URL
and no implementation source: the control-sequence reference page and the colored-and-
styled-underlines page. Those account for 10 of the added-line hits and 20 of the gated-file
hits in the second class, appearing as `XTERM-CTLSEQS`/`KITTY-UNDERLINES` in markers plus
the wire contract's own `Xterm256` variant name and the term the control-sequence page
itself applies to the 90-97/100-107 range. `ECMA-48`, `UAX-11`, `UAX-29` and
`POSIX-WINSIZE` are standards with no implementation half. `TERM-UNICODE-CORE` I read as
the specification source in the repository the row names, not the repository's front page —
the front page is a project summary and states none of what the row asserts. The remaining
hits in that class (7 added, 16 gated) are the pinned engine library, already named
throughout at the merge base: in the added lines it is the CI cache key and materialize
step, the crate doc's toolchain requirement, and one property of its public API. That is a
build dependency and a library-mediated API fact, which CLEANROOM.md rule 2's
library-mediated clause covers; no behavior is attributed to it as a comparand.

The judgment call the previous record left for the maintainer — the conventional label for
the SGR 90-97/100-107 bright range, an unregistered project name entering the tree as
quoted terminology from a registered source — is unchanged by this diff. It stays open in
the same form; still not a blocker either way, and this change neither widens nor narrows
it.

### leak

`./tools/leak-scan.sh` → `leak-scan: clean`, exit 0. `./tools/check-claims.sh` →
`check-claims: clean`, exit 0. `cleanroom-gate --markers` exit 0 over the twelve gated
`.rs` files. The full gate is red only on this record's absence, and names this subject.

Verdict: PASS. No escalation — every behavior in this change traces to a permitted source,
and the supervision half introduces no terminal behavior of its own, taking every PTY fact
from the engine crate's typed API. The five findings above are wording and pointer
corrections to claims whose underlying behavior is true and tested; one of them (4) is a
previous record's finding still open inside this subject, and one (1) is the single place
I would want the maintainer to make a deliberate call rather than let it pass by default.
