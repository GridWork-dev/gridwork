subject: 8c950fb44a221d0cf72d1435d5df704ebd2eb2c7dd64e8a735b5d358fdef3acc
reviewer: fresh-context subagent lane — no exposure to the implementing session; no SPEC, PLAN, or kickoff document for this phase was read, and the gated diff was read cold from `git diff --no-renames main...HEAD -- crates/gridwork/src/tui.rs`
session: secondreader-tui-read-honesty-7200e008
date: 2026-08-13

# Second-reader record

## Scope

The single gated path this branch (`fix/read-honesty`) touches is
`crates/gridwork/src/tui.rs`. The diff replaces the ad hoc "loop until no
cursor" pagination in `read_projection_records` (and the separately inlined
copy in `refresh_board_projections`) with a shared, budgeted page walk
(`crate::page_step` / `crate::drain_projection`, added in the non-gated
`crates/gridwork/src/lib.rs`), and plumbs the resulting "did this read
actually reach the end, or did it stop short at the page budget" bit back
through every caller. The one behavioral change inside the gated file is
`refresh_terms`: `TermState.complete` was hardcoded `true` and now carries the
real `drained` flag. Every other call site (`refresh_queue`, `refresh_config`,
`refresh_hall_cost`) discards the new third tuple element unchanged.
`refresh_board_projections` drops its own inlined loop for the shared
`drain_projection` helper and folds `page.drained` into `state.complete` per
attempt. Subject recomputed and matched: `git diff --no-renames --name-only
main...HEAD | CLEANROOM_BASE="$(git merge-base main HEAD)"
./tools/cleanroom-gate.sh --subject` → this file's subject; `--markers` over
the same path list exits 0, reporting `crates/gridwork/src/tui.rs cites
POSIX-TERM` (the file's only marker, unchanged by this diff — a file-level
doc comment, not a new citation).

## Checks

- [x] citations — every non-obvious terminal behavior carries a `Derivation:`
      marker naming a permitted source, and the marker sits on the behavior it
      actually describes. `cleanroom-gate` already proved every cited ID
      exists in `docs/derivation/SPECS.md` or `docs/derivation/CAPTURES.md`;
      what it cannot check, and you can, is whether the citation is TRUE —
      that the code does what that source says, and came from it
- [x] framing — no source-derived framing in comments, names, or structure
- [x] references — terminal-tooling comparand class: 0 hits; capture-cited-by-
      path class: 0 hits
- [x] leak — `./tools/leak-scan.sh` green

## Notes

### citations

`tui.rs` carries exactly one marker (`tui.rs:3-7`, file-level doc comment,
unchanged by this diff): `Derivation: POSIX-TERM §11.1.3, §11.1.10, §11.1.11 —
a consumer attach is not the terminal's last file-description close or a
modem disconnect, so dropping and rebuilding client streams never retires the
hosted child.` The task directing this review flagged this specific citation
for verification, so I fetched `pubs.opengroup.org/onlinepubs/9799919799/basedefs/V1_chap11.html`
(the exact URL `SPECS.md`'s `POSIX-TERM` row names) directly rather than
trusting the row's paraphrase, and confirmed the three sections say what the
marker claims: §11.1.3 (The Controlling Terminal) states that only the close
of the *last* file descriptor in the system associated with a controlling
terminal is what may (unspecified whether it does) sever the controlling
relationship — a single consumer's attach/detach is not that event; §11.1.10
(Modem Disconnect) ties `SIGHUP` to a detected hardware modem-disconnect
condition, a distinct trigger from any close; §11.1.11 (Closing a Terminal
Device File) describes what happens on the *last* process's close, again not
a mid-lifetime consumer attach. All three sections sit inside XBD Chapter 11's
descriptive interface material, which is `POSIX-TERM`'s registered scope
(`SPECS.md` is explicit that this row covers the interface description, not
function semantics like `tcsetwinsize()`, which belongs to the separate
`POSIX-WINSIZE` row) — so the citation is scoped correctly as well as
textually true. TRUE, and sits on the behavior it describes (the file's
overall client-attach/rebuild-vs-hosted-child-lifetime posture).

That citation is orthogonal to this diff's actual content, which is kernel
projection pagination, not terminal-protocol behavior. I checked whether the
diff owes a *new* citation and found it does not: `page_step`, `Drained`/
`Short`/`Continue`, the page-count budget, and `TermState.complete`/
`BoardState.complete` all govern how many `KernelRequest::ListProjection`
pages get drawn and whether the caller may claim a total — bookkeeping over
this repository's own typed protocol against its own kernel, not a terminal
control sequence, key encoding, or PTY-lifecycle behavior. The same file's
existing marker already disclaims exactly this category ("PTY controls and
projection rows use this repository's own typed protocol and carry their own
generation/cursor recovery semantics"), and `cleanroom-gate --markers`
confirms no gated file in this diff is unmarked. No `Derivation: none` claims
are touched by this diff (`tui.rs` carries none; it carries the one real
citation only). No escalation.

### framing

Nothing in the diff reads as transcribed from another implementation. Names —
`PageStep`, `PageStep::{Continue,Drained,Short}`, `DrainedPages`,
`drain_projection`, `page_step`, `MAX_PAGES_PER_PROJECTION`,
`PROJECTION_PAGE_LIMIT` — describe exactly what they do in this crate's own
vocabulary. Comments explain this crate's own reasoning in first-party voice:
`tui.rs`'s "Was hardcoded `true`. The reader can now stop short, so this is
the answer it gave rather than the answer we wanted," and "Reset per attempt,
alongside the rows: a retry re-reads every kind, so a short read on a
discarded attempt must not survive into the one that is kept." Structurally
the change collapses two duplicated ad hoc loops into one shared, tested
helper — a plain internal deduplication, not a shape borrowed from a described
external implementation.

### references

Read the full diff (~170 lines) directly; every comment, identifier, and
string literal in it is first-party, self-referential prose about this
crate's own pagination bookkeeping.

Checked four registered `SPECS.md` names by name, each as a permitted source
consulted — not a query term (README.md §references: a name already
registered "is not a query term and is not redacted — cite it," and where a
name could name either the protocol or the implementation, saying which was
checked is the finding). For each, what was checked is whether the diff names
it at all, as either a `Derivation:` citation (the protocol) or an
implementation comparand — the only question either reading reduces to when
the count is zero:

- `TMUX-MANUAL` (tmux — the one manual-page row): 0 occurrences in the diff,
  as either a citation or a comparand.
- `ZELLIJ-KEYBINDS` / `ZELLIJ-PRESETS` (zellij — the two user-guide rows): 0
  occurrences, either reading.
- `KITTY-KBD` / `KITTY-GRAPHICS` / `KITTY-UNDERLINES` (kitty — the three
  protocol rows): 0 occurrences, either reading.
- `XTERM-CTLSEQS` (xterm — the control-sequences row): 0 occurrences, either
  reading.

None of the four is cited anywhere in this diff — the diff's only
`Derivation:` marker is the unchanged file-level `POSIX-TERM` one, confirmed
independently via `cleanroom-gate --markers` and the `citations` section
above — and none appears as an implementation comparand either, so this diff
raises no protocol-vs-implementation question for any of the four.

Two names from the original six-term terminal-tooling search have no row in
`SPECS.md` at all and are genuine query terms in the rule's sense, folded
into a class count rather than named:

- terminal-tooling comparand class (2 terms): 0 hits.
- capture-cited-by-path class (2 terms): 0 hits — this diff cites no capture
  at all, by ID or otherwise.
- comparand-phrasing class (11 terms): 0 hits.

No other project is named as a comparand anywhere in this diff.

### leak

`./tools/leak-scan.sh` → `leak-scan: clean`, exit 0.

Verdict: PASS. Zero blockers. The one real citation in the gated file
(`POSIX-TERM` on the file-level doc comment) checks out against the live
specification text at the exact sections cited, correctly scoped to
`POSIX-TERM`'s registered document rather than reaching into `POSIX-WINSIZE`
territory. The diff itself introduces no new terminal behavior and needs no
new citation — it is kernel-projection pagination bookkeeping, the category
the file's existing marker already disclaims as this repository's own typed
protocol. No source-derived framing, no comparand references, leak-scan
clean.
