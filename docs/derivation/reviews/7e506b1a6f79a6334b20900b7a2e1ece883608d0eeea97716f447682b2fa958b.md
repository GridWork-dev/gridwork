subject: 7e506b1a6f79a6334b20900b7a2e1ece883608d0eeea97716f447682b2fa958b
reviewer: fresh-context subagent dispatch, independent second-reader lane — no exposure to the implementing session; the diff read cold from `git diff --no-renames main...HEAD`
session: secondreader-7p-p16-r1-2026-08-09
date: 2026-08-09

# Second-reader record

## Scope

Five gated paths, the interned (compact-cell) wire shape adopted through the PTY host.
`crates/gwk-pty-host/src/wire.rs` is the substantive change: `WireScreen::snapshot` now
produces the interned frame (`PtyFrame::from_cells` — first-appearance style table plus
maximal same-style runs), and the delta path builds a batch-scoped style table as it
collects updates (`StyleInterner`, interning only cells that actually changed), with four
new size tests pinning the measured geometries against real serialization.
`crates/gwk-pty-host/src/publish.rs` recomputes its declaration-time budget arithmetic
for the interned blank form (O(rows), matching the kernel hub's own formula) and gains
the sixteen-declared-sessions floor test. `crates/gwk-pty-host/src/session.rs` widens one
pattern match to the new `CellsChanged` shape. `crates/gwk-pty-host/src/registry.rs` and
`crates/gwk-pty-host/tests/detach.rs` adapt test/helper code to the expanded-cells
accessor (`frame.cells()`); no marker moves. No registry row is touched.

## Checks

- [x] citations — every non-obvious terminal behavior carries a `Derivation:` marker
      naming a permitted source, and the marker sits on the behavior it actually
      describes. `cleanroom-gate` already proved every cited ID exists in
      `docs/derivation/SPECS.md` or `docs/derivation/CAPTURES.md`; what it cannot check,
      and you can, is whether the citation is TRUE — that the code does what that source
      says, and came from it
- [x] framing — no source-derived framing in comments, names, or structure
- [x] references — no naming of other projects as comparands, and no capture cited by
      path: captures are referenced by their registered ID and SHA-256, never otherwise.
      Record the finding as the CLASS searched and the HIT COUNT — never the query terms
      themselves. A name already registered in ../SPECS.md is a permitted source, not a
      query term: cite it, and say whether you checked the protocol or the implementation
- [x] leak — `./tools/leak-scan.sh` green

## Notes

Subject recomputed and matched: `git diff --no-renames --name-only main...HEAD |
./tools/cleanroom-gate.sh --subject` →
`7e506b1a6f79a6334b20900b7a2e1ece883608d0eeea97716f447682b2fa958b`, against a clean
working tree at HEAD `e4808a0c34564486b13cc7d9a9b7126205d0bcad`. `--markers` over the
same file list exits 0: `publish.rs`, `registry.rs`, `session.rs`, and `wire.rs` each
declare no derivation; `detach.rs` cites `POSIX-TERM` three times.

### citations

Each `Derivation: none` was re-checked against its file as it now stands at HEAD, not
against the diff alone.

**`crates/gwk-pty-host/src/wire.rs` (`Derivation: none` — the file that gained real
encoding logic this diff).** The declaration reads *"converts the engine crate's own
typed output into the wire contract's types field by field; no terminal-protocol byte is
parsed and no process is supervised here."* Both halves re-verified against the whole
file: no process is spawned, killed, or waited on anywhere in the module, and no byte of
terminal protocol is parsed — the tests write escape bytes into `gwk_pty::Grid` and the
ENGINE parses them; this module only converts the engine's typed `Frame`/`Row` output.
The new logic is wire-format compression over this repository's own types, and each
load-bearing claim it makes was checked:

- *"interned into the wire form: a first-appearance style table, maximal same-style runs
  per row (the `from_cells` canonical form)"* (on `snapshot`) — verified against
  `crates/gwk-domain/src/frame.rs`: `PtyFrame::from_cells` interns first-appearance,
  scans maximal same-style runs, and its own doc pins the canonical form ("adjacent runs
  never share a style"). TRUE.
- *"The batch's own style table, built as the updates are: every message self-contained,
  no index space outliving it."* — verified: a fresh `StyleInterner` per `apply` call,
  its table moved into that batch's `PtyDelta::CellsChanged { styles, .. }`; the domain
  type's doc states the same batch-scoped rule. TRUE.
- *"Only a CHANGED cell interns its style, so the batch table carries exactly the styles
  its updates reference."* — verified in `set_cell`: the intern happens after the
  unchanged-cell early return, and the unchanged-screen test asserts an empty table.
  TRUE.
- The new attr-heavy size test writes an SGR pile as INPUT to the real engine parser and
  asserts only serialized byte counts — first-party measurements (3,455 and 2,716,431
  bytes, both pinned exactly with re-measure dates). The terminal-protocol facts the
  fixture leans on are not asserted here and not uncited in the repo: the identical
  attribute pile is pinned, with its own `Derivation:` markers, in the engine crate's
  gated style tests, which this file's pre-existing fixture-not-a-claim comment
  (`styles_ride_the_conversion`) already names as the citation home. The module encodes
  no terminal behavior a permitted source would have to be named for HERE. The `none`
  declaration REMAINS true.

**`crates/gwk-pty-host/src/publish.rs` (`Derivation: none`).** *"channel plumbing and
typed client calls only; no terminal byte is parsed and no process is supervised here"*
— TRUE at HEAD: env-string parsing, serialized-size arithmetic over domain types, and
typed `KernelClient`/`Attacher` calls. The rewritten `blank_grid_bytes` claims were
checked individually:

- *"by the same arithmetic as the kernel hub's own `blank_grid_bytes` under the interned
  shape"* — verified against `crates/gwk-kernel/src/wire/pty.rs`: the kernel's function
  computes the identical `(style + 22) + rows × (fill + 3)` with the same measured blank
  style and measured fill run. TRUE.
- *"its `count` is `cols`, so the digits are right by construction"* — TRUE: the
  measured `PtyRun::Fill` is built with `count: u32::from(cols)`, so the serialized
  digit width matches the real frame's.
- *"22 punctuation bytes outside the per-row term"* — independently recomputed from the
  JSON shape: 23 fixed envelope bytes with the per-row term absorbing one comma per row
  (rows − 1 present), netting exactly `22 + rows × (fill + 3)`. TRUE, and exact — the
  measured fill/style terms make the formula the real serialization, not an estimate,
  given `from_cells` emits exactly one fill run per all-blank row (verified in
  `frame.rs`).
- *"`parse_sessions` refused zero dimensions before this is ever asked"* — TRUE: the
  `cols == 0 || rows == 0` refusal precedes the budget check.

**`crates/gwk-pty-host/src/session.rs` (`Derivation: none`).** The declaration openly
names what the file does — *"process supervision and channel plumbing over the engine
crate's own API; no terminal-protocol byte is parsed here (the engine's parser owns
that) and every PTY fact used is one the engine already exposes as typed output"* — and
that is what the file at HEAD contains: `pump_chunk`/`wait`/`kill`/`send`/`resize`/
`grid` are all the engine crate's typed surface, and the diff is a one-line pattern
widening (`CellsChanged { updates, .. }`) that asserts nothing new. TRUE.

**`crates/gwk-pty-host/src/registry.rs` (`Derivation: none`).** *"a map of task handles
and channel plumbing; nothing here touches a terminal byte or a process directly (that
is `crate::session`'s task)"* — TRUE at HEAD: this diff touches only the test module
(the consumer model now resolves each update through the batch's own style table and
expands snapshots via `frame.cells()`), process creation stays behind the engine crate's
`Session::spawn` reached through the `SpawnFn`, and byte handling stays in the session
task. The attach-boundary prose and the rest of the non-test file are unchanged by this
diff. Tests still spawn only `/bin/sh`, `/bin/cat`, and `/bin/true`, honoring the
no-real-engine rule the test module states.

**`crates/gwk-pty-host/tests/detach.rs` (three `POSIX-TERM` markers).** This diff
changes only the `rows_text` helper (the `frame.cells` field read becomes the
`frame.cells()` expansion); no marker, assertion, or script line moves. Each marker was
re-checked positionally against the file at HEAD: §11.1.3 sits on the module doc and
governs the whole-file demonstration it describes (a detach performs none of the
association-moving events the chapter enumerates, labeled as this crate's reading);
§11.1.11 sits directly on the `ACK:pending` await it licenses (the discard is bound to
the last close, and a detach closes no descriptor); §11.1.10 sits directly on the
`GOT-HUP`/`SAW-EOF` absence assertions it licenses (no disconnect detected, so neither
consequence). `POSIX-TERM` resolves to POSIX.1-2024 XBD §11 in `../SPECS.md`. All three
TRUE — the markers still sit on the behaviors they describe, and the quoted fragments
are byte-identical to the merge-base text this diff left untouched.

### framing

First-principles throughout. The interning design is argued from this repository's own
measurements (per-cell style cost, the publish budget) and its own wire-contract
vocabulary (`styles` table, `Fill`/`Cells` runs, batch-scoped index spaces); the new
test names describe measurements and floors, not another implementation's features; the
comments cite the crate's own spike numbers with dates and pin them as exact
assertions. Nothing in the gated diff reads as transcribed from another implementation
rather than from the domain contract and first-party measurement.

### references

Class and hit count only, per this directory's rule. Searched all five gated files in
full at HEAD. Term lists were positive-controlled against a seeded scratch file outside
the repository before any count was trusted — 2 of 2 control hits — and the scratch
files and term lists were deleted after the run.

- terminal-multiplexer / terminal-tooling comparand class (10 terms, case-insensitive
  substring): 92 raw matches, 0 comparand references. Every one is the common English
  display-surface noun in ordinary prose and first-party names (including the crate's
  own `WireScreen` type), spread across all five files.
- registered `../SPECS.md` multiplexer/terminal names (4 terms): 6 raw hits, all in
  `wire.rs`, all the wire contract's own `Xterm256` variant name — xterm is a registered
  permitted source (`XTERM-CTLSEQS`), so it is named here plainly: the hits are the
  domain type's palette-tier name (the 256-color palette convention baked into a
  first-party identifier), not a citation and not a comparand; neither the protocol
  document nor any implementation is consulted or referenced by this file, and where
  the SGR facts themselves need a citation, the engine crate's gated tests carry it
  against the protocol document.
- capture-cited-by-path class (2 patterns): 0 hits. No capture is cited in this diff at
  all, by ID or otherwise.

### leak

`./tools/leak-scan.sh` → `leak-scan: clean`, exit 0. `./tools/check-claims.sh` →
`check-claims: clean`, exit 0.

Verdict: PASS. Zero blockers. The four `none` declarations all remain true at HEAD —
wire.rs, the one file that gained real encoding logic, gained wire-format compression
over first-party types, with the terminal-protocol facts its new fixture leans on
asserted and cited in the engine crate rather than smuggled in here; publish.rs's new
budget arithmetic was recomputed independently and is exact, matching the kernel hub's
own formula; session.rs and registry.rs carry mechanical adaptations only. The three
`POSIX-TERM` markers in detach.rs are untouched by this diff and still sit on the
behaviors they license.
