subject: 6c756f16e9edac7e0479d2da4453c346f93b0141e58a4f634a2fbc04a28f1260
reviewer: fresh-context subagent dispatch, independent second-reader lane — no exposure to the implementing session; no SPEC or PLAN document for this phase was read, and the diff was read cold from `git diff main...HEAD`
session: 018D1MbfADJLDWzMw9uuHF23
date: 2026-08-04

# Second-reader record

## Scope

Four gated paths, all under `crates/gwk-pty/`: the render delta gains typed per-cell
style. `src/style.rs` is new — a `CellStyle` struct of twelve attributes, a `Color`
enum (palette index or direct RGB) and an `Underline` enum (five shapes), plus a
`From<VtStyle>` conversion off the engine's own per-cell style query. `src/render.rs`
adds a `styles: Vec<CellStyle>` field to `Row`, populated in the same iteration that
pushes each cell's cluster into `text`. `src/lib.rs` adds the module and three
re-exports. `tests/style.rs` is new: 25 fidelity tests. Two registry rows are added
(`KITTY-UNDERLINES` in `SPECS.md`, `CAP-004` in `CAPTURES.md`, with its capture file
committed under `docs/derivation/captures/`) and one row is amended
(`TERM-UNICODE-CORE`), which is why `src/lib.rs` — whose own bytes are otherwise a
two-line edit — and every other citing file are in-subject.

This is the branch's second pass. A record for an earlier subject was written and then
removed by the two `fix(pty)` commits that changed the content it was bound to; that is
the mechanism working, and this record is bound to the current content only.

## Checks

- [x] citations — every non-obvious terminal behavior carries a `Derivation:` marker
      naming a permitted source, and the marker sits on the behavior it actually
      describes. `cleanroom-gate` already proved every cited ID exists in
      `docs/derivation/SPECS.md` or `docs/derivation/CAPTURES.md`; what it cannot check,
      and you can, is whether the citation is TRUE — that the code does what that source
      says, and came from it
- [x] framing — no source-derived framing in comments, names, or structure
- [x] references — no naming of other projects as comparands, and no capture cited by
      path: captures are referenced by their registered ID and SHA-256, never otherwise
- [x] leak — `./tools/leak-scan.sh` green

## Notes

Subject recomputed and matched:
`git diff --no-renames --name-only main...HEAD | CLEANROOM_BASE="$(git merge-base main HEAD)" ./tools/cleanroom-gate.sh --subject`
→ `6c756f16e9edac7e0479d2da4453c346f93b0141e58a4f634a2fbc04a28f1260`, against a clean
working tree at HEAD `549c100` (merge base `55531af`). The gate names exactly four
gated paths and reports the three registry rows it is treating as touched. No file in
this change carries a `Derivation: none` declaration, so the `none`-is-a-claim check has
no subjects here; every marker is a positive citation and each was read against its
source.

### citations — every cited source read at its registered URL, not inferred

`ECMA-48` was read as the 5th-edition PDF the registry's page links (354 pages,
extracted with `pdftotext -layout`), not from a secondary rendering. `§8.3.117` is
indeed SGR, and every parameter gloss the change puts inside quotation marks is verbatim
against it, checked one at a time:

- `src/style.rs` — 1 "bold or increased intensity", 2 "faint, decreased intensity or
  second colour", 3 "italicized", 4 "singly underlined", 21 "doubly underlined", 7
  "negative image", 8 "concealed characters", 9 "crossed-out", 53 "overlined". All
  exact, British spelling preserved. 9's quote truncates before its parenthetical, which
  drops no meaning.
- `tests/style.rs` — additionally 22, 23, 24, 27, 29, 31, 39, 44, 49, 5, and 0. All
  exact. The SGR 0 quote stops before "regardless of the setting of the GRAPHIC
  RENDITION COMBINATION MODE (GRCM)"; the dropped clause strengthens rather than
  qualifies the claim, so the truncation is not distorting.
- The reserved-parameter claim under `underline_color` is right: §8.3.117 lists 58 and
  59 as "(reserved for future standardization)" and assigns them nothing, which is
  exactly what makes the extension load-bearing there.
- `tests/style.rs:261-268` is the strongest citation in the change. §8.3.117 really does
  say the established aspects remain in effect "depending on the setting of the GRAPHIC
  RENDITION COMBINATION MODE (GRCM)", and §7.2.8 really does define exactly two
  settings, REPLACING and CUMULATIVE. The comment states the dependency, names the
  setting the test relies on, and says the assertion is what pins it. That is the
  correct shape for a conditional spec fact.
- `§8.3.9` (CHA), `§8.3.20` (CUF) and `§8.3.21` (CUP), cited from `src/lib.rs` and
  `src/render.rs`, all resolve to those functions with the stated 1-based parameters and
  `Pn = 1` defaults.

`XTERM-CTLSEQS` confirms the section heading quoted, the bright-eight range as an
extension of the same shape, both `38 : 5 : Ps` indexed and `38 : 2 : Pi : Pr : Pg : Pb`
direct forms with their 48 counterparts, and that colons or semicolons may separate
sub-parameters — so the semicolon spellings in the markers are documented, not assumed.
Its own line for parameter 21 is "Doubly-underlined, ECMA-48 3rd.", independently
agreeing with the double-underline reading the change pins.

`KITTY-UNDERLINES` confirms `4:3` curly, `4:4` dotted, `4:5` dashed exactly as listed,
and its sentence about 58 reads "This works exactly like the codes 38, 48 that are used
to set foreground and background color respectively" — the fragment inside quotation
marks in `src/style.rs:77-79` is a verbatim substring of it.

`UAX-11` is the one I expected to find overreaching and did not. The annex does say Wide
characters "take up one Em of space" in fixed-pitch fonts against "1/2 Em" for the
halfwidth and narrow classes, and it does carry the note that the property "is not
intended for use by modern terminal emulators without appropriate tailoring on a
case-by-case basis". The marker paraphrases that note without quotation marks and then
says outright that "two columns" is the crate reading the ratio into a cell grid and
NOT a sentence the annex contains — cite it for the ratio, not the conclusion. That is a
citation policing its own strength, and it is correct.

`TERM-UNICODE-CORE` was read as the specification source in the repository the registry
row names, not the repository's front page. Everything the amended row asserts checks
out: it is watermarked Draft and dated "2021-09-06 (draft, revision 1)"; the mode is
2027; the repository declares no license (the hosting API returns no license object and
the license endpoint 404s — verified independently of the row's claim). Its Semantics
section is explicitly conditional — the requirements "MUST be adhered to if this VT mode
is enabled", and "if the VT mode is not set, then the behavior is as undefined as if
this specification was not implemented at all". The marker in `src/render.rs:29-32`
tracks that split precisely and refuses the reset half, which is the correction the
`CAP-004` note describes. The correction is real, not asserted.

`CAP-004` verifies end to end. The committed capture hashes to
`bfe0f1ac…1d31e2bcaf3`, matching its row byte for byte. Its probe uses this crate's
public API and the same helper `tests/style.rs` uses, so it is re-runnable rather than
believed. Its arithmetic is internally consistent and I checked it: on a 10-column grid
a wide emoji is two columns and a spacer contributes no entry, so two clusters plus six
blanks is 8 and one cluster plus eight blanks is 9 — exactly the two lengths recorded.
I rebuilt against the pinned toolchain and ran the suite: 25 passed, 0 failed. The two
segmentation tests are mutually discriminating — each would fail if the engine behaved
the way the other pins — so they genuinely pin both sides of the condition rather than
restating it.

**Findings (three, non-blocking, all in the same family: a citation slightly stronger
than its source).** None asserts a behavior the code does not have, and none names an
impermissible source; each is a wording fix.

1. **`crates/gwk-pty/tests/style.rs:305-307`** — "an extended grapheme cluster does not
   break before or after ZERO WIDTH JOINER (rules GB9/GB11)". GB9 is unconditional and
   supports the "before" half. GB11 is
   `\p{Extended_Pictographic} Extend* ZWJ × \p{Extended_Pictographic}` — the "after"
   half holds only between extended pictographics, and GB999 breaks after a ZWJ in the
   general case. The conclusion drawn for THIS input is correct (both codepoints are
   extended pictographic), but the intermediate sentence is unconditional where its
   source conditions it. Fix: name the condition, or attribute "before" to GB9 and
   "within an emoji ZWJ sequence" to GB11.
2. **`crates/gwk-pty/tests/style.rs:269-270`** — cites `KITTY-UNDERLINES` alone for "SGR
   58 with the colon-separated `2` sub-parameter form sets the underline's own
   direct-RGB color". That page documents `<ESC>[58...m` and "works exactly like the
   codes 38, 48"; it never shows a sub-parameter form. The `2`-means-direct-RGB fact and
   the empty colour-space slot the test actually writes are `XTERM-CTLSEQS`'s
   (`Ps = 3 8 : 2 : Pi : Pr : Pg : Pb`). The inference is sound because of the "exactly
   like" sentence, so this is an incomplete citation rather than a false one — but the
   missing link is a source the same file already cites four times. Fix: add
   `XTERM-CTLSEQS` to that marker. Its counterpart at `src/style.rs:76-81` is complete
   on the colour-setting half and is the model.
3. **`crates/gwk-pty/src/style.rs:24-31`** — concludes "all three select a SLOT, not a
   colour". §8.3.117 names those parameters as colours ("31 red display", "44 blue
   background"); it nowhere describes them as index selectors. The slot reading is
   `XTERM-CTLSEQS`'s 256-entry table generalized backwards over the ECMA-48 range. The
   marker is transparent about the inferential step immediately before ("which is what
   makes them palette lookups"), and the actual index mapping is pinned by test against
   the engine rather than claimed from the spec, so no behavior claim is wrong — but the
   flat sentence attributes a characterization to ECMA-48 that ECMA-48 does not make.
   `tests/style.rs:110-111` gets this right, calling 31 "one of the eight basic named
   colours"; recommend aligning the enum marker to the test file's wording.

Three precision notes that are not findings, recorded so the next reader does not
re-derive them:

- `CAPTURES.md`'s `CAP-004` note says the specification "says three separate times that
  the unset state is undefined". I count four (Backwards Compatibility; the Mode
  Switching bullet; the Semantics preamble; Terminal Reset). Understated, not
  overstated.
- The same note says "its only sentence about joined sequences". There are three such
  sentences in that section. All sit inside the mode-enabled requirement and all run the
  same way, so the substantive claim — that the document cannot be cited for the reset
  half — holds unchanged.
- `src/render.rs:29-30` says the mode "requires cells to be extended grapheme clusters".
  The source says "grapheme clusters in conformance to" UAX-29 without the word
  "extended"; UAX-29 recommends the extended variant, so this is the right reading, but
  it is a reading rather than a quotation.

One quality nit outside the clean-room scope: `tests/style.rs:15-17` says "Two columns
is enough for every case below" while `styles_of` builds a ten-column grid. Stale
wording; no behavior claim rides on it.

### framing

No source-derived framing. Every shape name in `Underline` (`Curly`, `Dotted`,
`Dashed`) is the word the registered specification uses for that sub-parameter, which is
the correct provenance rather than a borrowed one. `dim` is explicitly flagged as this
crate's own word for the attribute the engine dependency names differently — a
disclosure, not a transcription. The struct's field set necessarily tracks the
dependency's public style type because it is a `From` conversion off it; that dependency
is consumed through its published safe API, which CLEANROOM.md rule 2's
library-mediated clause covers. The `_ => None` arm carries a `ponytail:` comment
explaining it as a `#[non_exhaustive]` requirement, not a behavior. Nothing in names,
comments or structure reads as transcribed from another implementation.

### references

Class and hit count only, per this directory's rule. Each class was run over two corpora
— the added lines of the full diff, and the four gated files at HEAD — and the scanner
was positive-controlled against seeded strings from every class before any zero was
trusted.

- terminal-multiplexer / session-manager comparand class (11 terms): 0 hits
- terminal-emulator / VT-implementation comparand class (26 terms): 1 raw hit in the
  added lines, 0 in the gated files — the hosting organization inside the new
  `TERM-UNICODE-CORE` row's URL. That is a registered permitted source as of this
  change, so it is cited rather than redacted, and the protocol-versus-implementation
  question it raises is answered below. 0 comparand uses.
- PTY-supervision-tool comparand class (13 terms): 8 raw hits in the added lines and 26
  in the gated files, every one the Rust `.expect(...)` method or the word "expected"
  inside an assertion message. 0 project-name uses.
- derivation-by-comparison phrasing class (14 terms): 0 hits
- capture-cited-by-path class (2 patterns): 2 hits, both the diff's own `+++ b/` file
  header for the newly added capture — an artifact of scanning diff text, not a
  citation. 0 hits in the gated files. No `Derivation:` marker and no prose cites a
  capture by path; `CAPTURES.md` names the captures directory, which that file's own
  text explicitly permits and requires.

Registered permitted sources, and which half of each I checked: for the three registered
names that are also implementations, I read the **protocol document** at the registered
URL and no implementation source — the control-sequence reference page, the colored-and-
styled-underlines protocol page, and the specification source in the terminal-unicode-core
repository. `ECMA-48`, `UAX-11` and `UAX-29` are standards with no implementation half.
The engine library this crate builds on is named throughout as a build dependency, as it
already is at the merge base; no behavior is attributed to it as a comparand.

One judgment call recorded rather than buried. `src/style.rs:28` and
`tests/style.rs:112` introduce, for the first time in this repository, the conventional
label for the SGR 90-97/100-107 bright range. That label is not a comparand — it is the
term `XTERM-CTLSEQS` itself applies to those parameters in its own text, and no behavior
is attributed to the tool it names — but it is an unregistered project name entering the
tree, so the maintainer should decide deliberately between keeping it as quoted
terminology from a registered source and rewriting the two comments to describe the
range instead. Not a blocker either way.

A second judgment call, on the `TERM-UNICODE-CORE` row itself. `SPECS.md` requires every
row to be a public, freely-obtainable document and calls a paywalled, leaked or
copyleft-licensed one a rule 1 violation. This document is publicly readable and is none
of those three, but it carries no license at all, which the row states plainly rather
than glossing. Rule 1 governs copied code and nothing is copied here; the row is cited
for what it says, and it discloses its own draft status and license state so an auditor
can judge. I record it as a disclosed residual, not a finding.

### leak

`./tools/leak-scan.sh` → `leak-scan: clean`, exit 0. `./tools/check-claims.sh` →
`check-claims: clean`, exit 0. The full gate is red only on this record's absence, and
names the four gated paths and this subject.

Verdict: PASS. No escalation — every behavior in this change traces to a permitted
source, and the one behavior with no citable specification (cell segmentation under the
reset mode) is registered as this repository's own re-runnable observation rather than
attributed to a document that declines to state it. The three findings above are wording
corrections to markers whose underlying behavior claims are all true and all tested.
