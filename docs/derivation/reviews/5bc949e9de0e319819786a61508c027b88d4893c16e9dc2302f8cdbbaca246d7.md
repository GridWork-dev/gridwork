subject: 5bc949e9de0e319819786a61508c027b88d4893c16e9dc2302f8cdbbaca246d7
reviewer: gw-general-purpose, fresh-context dispatch — no exposure to the implementing session (routed here per CLEANROOM.md rule 4's requirement for a fresh-context lane; every specialized GridWork agent carries house instructions that would be exactly the prior exposure the control exists to exclude)
date: 2026-08-03

# Second-reader record

## Scope

The six gated `gwk-parity` files (`src/bin/{parity,parity-claude-hook}.rs`,
`src/runners/{mod,claude,codex,opencode}.rs`) gain their first `Derivation:` markers —
ten citations across three registered sources plus two `none` declarations — and
`tools/cleanroom-gate.sh`'s shared marker extractor gains the `///` outer-doc comment
form, seeded in CI. The same change (outside the gated set) rebuilds the site docs
freshness gate around in-mirror `curated-from` bindings with per-page byte registration
and directory-listing digests.

## Checks

- [x] citations — every non-obvious terminal behavior carries a `Derivation:` marker
      naming a permitted source, and the marker sits on the behavior it actually
      describes. `cleanroom-gate` already proved every cited ID exists in
      `docs/derivation/SPECS.md` or `docs/derivation/CAPTURES.md`; what it cannot check,
      and you can, is whether the citation is TRUE — that the code does what that source
      says, and came from it
- [x] framing — no source-derived framing in comments, names, or structure
- [x] references — no naming of other projects as comparands, and no capture cited by
      path: captures are referenced by their registered ID and SHA-256, never otherwise
- [x] leak — `./tools/leak-scan.sh` green

## Notes

Subject computed cold before reading the diff:
`git diff --no-renames --name-only main...HEAD | ./tools/cleanroom-gate.sh --subject` →
`5bc949e9de0e319819786a61508c027b88d4893c16e9dc2302f8cdbbaca246d7`; identical without
`--no-renames` (the diff renames nothing). Six gated paths; the marker inventory the
gate reports (1× CLAUDE-HOOKS, 1× CLAUDE-HOOKS, 4× CODEX-APP-SERVER,
5× OPENCODE-SERVER, 2× `none`) matches what I verified below, marker for marker.
`./tools/leak-scan.sh`: clean. `./tools/check-claims.sh`: clean.

### Every citation verified against its primary source

- **CODEX-APP-SERVER (4 markers, `runners/codex.rs`)** — verified against the
  registered source itself: I ran `codex app-server generate-json-schema` against the
  installed pinned binary (codex-cli 0.146.0, the exact pin `src/pins.rs` names) and
  read the emitted files the markers cite. All four hold field-for-field:
  `ClientRequest.json` has the `initialize`, `thread/start`, and `turn/start` variants,
  each `{id, method, params}` all required; `v1/InitializeParams.json` requires
  `clientInfo` and `ClientInfo` requires `name` + `version`; `v2/ThreadStartParams.json`
  has NO `required` list and every property nullable, so `{}` is a legal params body,
  and its `#/definitions/AskForApproval` carries `"untrusted"` and
  `#/definitions/SandboxMode` carries `"read-only"`; `v2/ThreadStartResponse.json`
  requires `thread` and its `Thread` carries a required `id`;
  `v2/TurnStartParams.json` requires exactly `input` + `threadId`, `input` is
  `[UserInput]`, and `UserInput`'s text variant requires `type` (enum `"text"`) +
  `text` in `ClientRequest.json`'s `#/definitions/UserInput`. The module doc's
  "live-discovered" initialize-first requirement is additionally documented in the
  row's README half verbatim ("requests issued before initialization receive a
  `"Not initialized"` error"), so nothing rests on an unregistered observation.
- **OPENCODE-SERVER (5 markers, `runners/opencode.rs`)** — endpoint-table claims
  verified on the registered docs page (`GET /doc` "OpenAPI 3.1 specification";
  `GET /event` "Server-sent events stream. First event is `server.connected`, then bus
  events"; `POST /session` body `{ parentID?, title? }` returning `Session`;
  `POST /session/:id/prompt_async` returning `204 No Content`;
  `GET /session/:id/children` returning `Session[]`). Body-shape claims verified
  against the OpenAPI 3.1 schema served at `GET /doc` by a throwaway local
  `opencode serve` at the pinned 1.18.3 (read-only probe, no session created, server
  killed after — the same procedure the round-2 opencode-adapter record used):
  `POST /session` has no required field anywhere (`{}` legal) and returns a `Session`
  whose `required` includes `id`; `prompt_async` requires `parts`, whose
  `TextPartInput` requires `type` (enum `"text"`) + `text`, and answers `204`;
  `children` answers `Session[]` ("List of children"). This checked the PROTOCOL
  surface (docs + published schema) only; no implementation source of any engine was
  read.
- **CLAUDE-HOOKS (2 markers, `bin/parity-claude-hook.rs` + `runners/claude.rs`)** —
  the cited hooks reference documents all seven fields the fixture instantiates
  (`session_id`, `transcript_path`, `cwd`, `hook_event_name`, `tool_name`,
  `tool_input`, `tool_use_id` — the last shown in the page's own PreToolUse example),
  and the hook contract the binary enacts: stdin JSON in, decision JSON as the hook's
  own stdout, JSON processed only on exit 0. One minor, non-blocking imprecision: the
  marker's quoted phrase "Exit 0 with JSON for structured control" is a near-paraphrase
  of the page's current wording ("exit 0 and print JSON for structured control") inside
  quotation marks that overclaim verbatimness. The substantive claim is fully
  substantiated; worth dropping the quotes on a future touch.
- **The two `none` declarations, checked as claims.** `bin/parity.rs` is 27 lines:
  calls `run_all()`, prints, exits non-zero on non-green — spawns nothing in-file,
  parses no byte; true as written. `runners/mod.rs` is the one that earns the harder
  look: the file DOES spawn engine CLIs (`<binary> --version` in `check_version`), so
  the reviews-README heuristic ("does it spawn a process…") fires. Scrutinized against
  rule 3, the declaration holds: the spawned interaction is a version-string substring
  check — no wire shape built, no protocol byte parsed, no behavior with any permitted
  source to cite — and the declaration's own reason names the version-pin checks
  rather than hiding them. Ruling: a truthful `none`, recorded with the reasoning so
  the next reader need not re-derive it. Had the reason said "skeleton only", it would
  have been false.

### The other three checks

- framing: names and structure mirror the cited schemas' own vocabulary
  (`thread/start`, `parts`, `prompt_async`, the `{id, method, params}` frames); the
  module docs state the derivation procedure itself (the generation command against the
  pinned binary; `GET /doc` fetched the way the adapter's own citations were) and are
  explicit about which asks are adapter-constructed rather than engine-triggered. Pass.
- references: terminal-tooling comparand class (21 terms): 0 hits across the full
  diff. No capture cited by path (the captures-directory path pattern: 0 hits in the
  diff). The engine names that do appear are registered protocol sources under
  adaptation, not comparands; per above, I checked their protocol surfaces, not any
  implementation. Pass.
- leak: `./tools/leak-scan.sh` → "leak-scan: clean"; `./tools/check-claims.sh` →
  "check-claims: clean". Pass.

### Adversarial probes on the changed gate logic (all cleaned up; porcelain clean after each)

The diff edits the gate that checks it, so the seeds were not trusted; every probe
below was written and run fresh in this review.

Marker extractor (the widened alternation):

1. `/// Derivation: <cite>` alone → accepted (the fix works).
2. `///Derivation: <cite>` (zero spaces after the comment token) → accepted; still a
   marker.
3. `/// Derivation: none — reason` beside `// Derivation: <cite>` → never-both
   rejection fires across the two forms. The hole the diff names is closed.
4. **Residual finding (non-blocking, recorded):** `//// Derivation: <real cite>`
   beside `// Derivation: none — reason` passes as a clean declaration. Four slashes
   is invisible to the extractor, so the `none` stands alone — the
   reviewer-stops-at-the-none direction, one unrecognized form over. Why not a
   blocker: rustdoc treats four-plus slashes as a plain comment, not a doc comment,
   so it is not a form where a marker naturally lands (a typo is the realistic
   entry); the closed form-set is the extractor's documented design (`/*` is excluded
   on the same grounds); and a `none` already obliges rule 4's reader to read the
   file, where the stray line is visible. Why recorded anyway: the gate comment's
   form list ("`//`, `//!`, `///` and `#`") invites the assumption that any
   slash-run is covered. A future touch could widen the alternation to a slash-run or
   seed the exclusion the way `/*` is documented.
5. Pre-existing observation (not this diff's): `Derivation: none —` with only an
   em-dash for a reason satisfies the mandatory-reason pattern (any one non-space
   character does). Visibly empty to a reader; noted for a future tightening.

Site freshness gate (independent re-derivations of the three seeds, plus three the
seeds do not cover):

1. Canonical source appended, mirror untouched → red through the mirror's own
   `curated-from` binding (the incident shape).
2. Mirror content appended without re-registration → red through the page-byte
   registration.
3. New file in a SUBDIRECTORY of the reviews tree (the CI seed only drops a top-level
   dotfile) → red through the directory listing digest; `recursive: true` holds.
4. Empty subdirectory added → green. Correct: the digest binds file bytes and paths;
   an empty directory carries neither.
5. Content SWAP between two existing records (the hash SET is unchanged; only the
   hash↔path assignment moves) → red. The `<sha256>  <relpath>` line pairing is what
   catches this; a set-of-hashes digest would have passed it.
6. Unmapped mirror page added → red through the both-directions coverage check.

I also attempted to construct a stale-mirror-passes case and could not: with the
source digest living inside the mirror and the mirror's own bytes registered in the
map, every reconciliation path requires editing the curated page itself — there is no
third artifact left to refresh around it. The one accepted path (update the binding
digest + re-register, content unchanged) edits and signs the mirror file, which is the
design's stated bargain, not a bypass.

### Expected downstream effect, stated plainly

Committing this record adds a file under `docs/derivation/reviews/`, which moves that
directory's listing digest, so this branch's own new site binding goes red on the
reviews mirror until it is re-curated. That is the binding doing its job; the
re-curation is downstream work outside this record's scope.

Verdict: PASS.
