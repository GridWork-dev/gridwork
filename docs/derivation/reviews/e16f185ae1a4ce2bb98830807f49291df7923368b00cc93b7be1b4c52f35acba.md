subject: e16f185ae1a4ce2bb98830807f49291df7923368b00cc93b7be1b4c52f35acba
reviewer: fresh-context general-purpose second-reader lane with no exposure to the implementing session or its private execution briefs
session: fd57ec5c-1fb3-4eac-84f6-ffdc3a53585f
date: 2026-08-12

# Second-reader record

## Scope

Reviewed the gated committed changes to `crates/gridwork/src/tui.rs` and
`crates/gwk-tui/src/workspace/{arrange,input,mod,render,runtime}.rs`, which add
durable multi-pane workspace wiring, PTY stream routing and input encoding, and
related model and rendering updates.

## Checks

- [x] citations — every non-obvious terminal behavior carries a truthful,
      correctly placed `Derivation:` marker; all `Derivation: none` claims are
      accurate
- [x] framing — no source-derived framing in comments, names, or structure
- [x] references — terminal-tooling comparand class: 0 hits; capture-path
      citation class: 0 hits
- [x] leak — `./tools/leak-scan.sh` green

## Notes

- `KITTY-KBD` legacy mapping matches the committed runtime mapping: listed ASCII
  keys produce the documented C0 or DEL bytes, including `~` to 30, while
  unlisted ASCII remains unchanged.
- The specification assigns Ctrl+Shift text keys to CSI-u. Refusing them because
  CSI-u was not negotiated avoids emitting unsupported bytes.
- `TMUX-MANUAL`, `ZELLIJ-PRESETS`, and `ZELLIJ-KEYBINDS` occurrences are
  registered public-document citations, not implementation comparands.
- The marker gate resolved all six gated files and reproduced this subject.
- The targeted encoding test passed: 1 passed, 0 failed.
