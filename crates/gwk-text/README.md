# gwk-text

Pure text primitives for GridWork: column arithmetic and extended grapheme
boundaries. The crate accepts UTF-8 text and returns values; it performs no I/O,
spawns no process, and emits or parses no terminal-protocol bytes.
